# smile — سند معماری

> یک مغز دیجیتال رویدادمحور. نه یک مدل زبانی، نه یک شبکه‌ی عصبی کلاسیک.
>
> **وضعیت:** پیش‌نویس ۰٫۳ — حاصل گفتگوی طراحی. هنوز هیچ کدی نوشته نشده.
> این سند مرجعِ مشترک ماست؛ هر جا اشتباه فهمیدم، همین‌جا اصلاح می‌شه.
>
> **در نسخه‌ی ۰٫۳:** ۱۵ ضعف شناسایی‌شده برطرف شد — فهرست کامل در بند ۱۵٫۱.

---

## ۰. تفاوت بنیادی با مدل‌های امروزی

| مدل‌های فعلی (ترنسفورمر) | smile |
|---|---|
| بین دو پرامپت مرده است | همیشه فعال، بی‌وقفه |
| با ورودی شروع، با خروجی تمام | جریان پیوسته‌ی داخلی |
| نورون = ضرب ماتریسی، بی‌اختیار | نورون = تصمیم‌گیرنده‌ی مستقل |
| حافظه = پنجره‌ی متن | حافظه = بایت‌های واقعی داخل هر نورون |
| هزینه‌ای برای محاسبه احساس نمی‌کند | انرژی محدود؛ گرسنگی و مرگ واقعی است |
| یادگیری = گرادیان سراسری | یادگیری = فشار اقتصادی محلی |

**اصل حاکم:** هیچ ناظر مرکزی‌ای وجود ندارد. هر رفتار جالبی باید از تصمیم‌های محلی نورون‌ها **پدیدار** شود.

---

## ۱. دو دنیای مجزا: مغز و دستگاه

```
╔══════════════════════════════════════════════════════════╗
║                        دستگاه                             ║
║        (بیرون از مغز — مغز از وجودش خبر ندارد)            ║
║                                                          ║
║   حواس · زبان · آینه · داور · محاسبگر · نگهبان           ║
╚══════════════════════════════════════════════════════════╝
                    │  ▲                    ▲
              ۲ بیت │  │ ۲ بیت              │ مانا
                    ▼  │                    │
╔══════════════════════════════════════════════════════════╗
║                         مغز                              ║
║                                                          ║
║   ┌───────────────┐   ┌──────────┐   ┌───────────────┐  ║
║   │   لوب ورودی   │──▶│   لوب    │──▶│  لوب پایانی   │  ║
║   │  الف: از انسان│   │  مرکزی   │   │               │  ║
║   │  ب: از آینه   │◀──│          │◀──│               │  ║
║   └───────────────┘   └──────────┘   └───────────────┘  ║
║          حلقه‌های داخلی در هر جهت آزادند                  ║
╚══════════════════════════════════════════════════════════╝
```

**قانون طلایی:** مغز نه دستگاه را می‌بیند، نه می‌تواند تغییرش دهد. تنها رابط، ۲ خط ورودی و ۲ خط خروجی است. به همین دلیل مغز نمی‌تواند سیستم پاداش را هک کند.

---

## ۲. نورون

### ۲٫۱ سه رده

| ویژگی | عادی | حافظه‌ای | غول‌پیکر |
|---|---|---|---|
| **سهم جمعیت** | ~۹۶٫۸۵٪ | ۳٪ | ۱ به ازای هر ۲۰ حافظه‌ای (~۰٫۱۵٪) |
| **خطوط ورودی** | ۲۰ | ۴۰ | ۶۰ |
| **خطوط خروجی** | ۲۰ | ۴۰ | ۶۰ |
| **حافظه** | ۳۲ بایت | ۱ کیلوبایت | ۴ کیلوبایت |
| **سقف مانا** | ۲۰ | ۴۰ | ۱۲۰ |
| **سوخت هر اجرا** | ثابت | متوسط، متغیر | زیاد، متغیر |
| **تابع اصلی** | بایت‌کد خطی (بدون `if`/حلقه‌ی درونی) | بایت‌کد کامل | بایت‌کد کامل |
| **محل اجرا** | GPU | CPU | CPU |
| **تابع ذخیره‌ساز** | ثابت (فاز ۱) | فقط **پارامترها** قابل تغییر | **کاملاً** قابل بازنویسی |

در شبکه‌ی ۱۰۰٬۰۰۰ تایی: ۹۶٬۸۵۰ عادی + ۳٬۰۰۰ حافظه‌ای + ۱۵۰ غول.
غول‌ها همراه شبکه رشد می‌کنند و نسبتشان همیشه ناچیز می‌ماند.

### ۲٫۲ اتصالات غول‌پیکر

از ۶۰ خط:
- **۲۰ خط** → حافظه‌ای‌های زیردست (قلمرو خودش)
- **۴۰ خط** → غول‌های دیگر

یعنی غول‌ها عمدتاً با هم مراوده می‌کنند و یک شبکه‌ی نخبه تشکیل می‌دهند.

### ۲٫۳ ورودی تابع تصمیم

هر بار که نورون تصمیم می‌گیرد، تابعش این‌ها را می‌بیند:

| ورودی | چرا لازم است |
|---|---|
| بیت‌های خطوط ورودی (۲۰/۴۰/۶۰) | داده‌ی اصلی |
| **بیت تازگی برای هر خط** | بدون این، نمی‌فهمد سیگنال همین‌الان رسیده یا مال ده ثانیه پیش است |
| حافظه‌ی داخلی خودش | حالت شخصی |
| **مانای فعلی خودش** | تا بتواند محتاط یا ولخرج باشد |
| **زمان سپری‌شده از آخرین فایر** | حس زمان |
| **نویز شبه‌تصادفی** | خودجوشی؛ بدون آن شبکه‌ی ساکت هرگز از جا تکان نمی‌خورد |

### ۲٫۴ خروجی تابع تصمیم

- فایر بکنم یا نه
- روی **کدام** خطوط بفرستم (زیرمجموعه‌ی دلخواه)
- چه بیتی روی هر خط
- حافظه‌ام را تغییر بدهم یا نه

**فایر انتخابی است.** نورون می‌تواند روی ۱ خط زمزمه کند (۰٫۲ مانا) یا روی هر ۲۰ خط داد بزند (۴ مانا). این یک تصمیم اقتصادی است، نه یک سوئیچ.

#### ✅ رفع ضعف: فروپاشی به پچ‌پچ

خطر: چون زمزمه ارزان است، ممکن است همه یاد بگیرند فقط ۱ خط بفرستند و شبکه به پچ‌پچی کم‌رمق تبدیل شود که هرگز به خروجی نمی‌رسد.

**مهار: هزینه‌ی راه‌اندازی + بازده فزاینده.**

```
هزینه‌ی فایر = ۰٫۵ (ثابت، صرف‌نظر از تعداد خط) + ۰٫۲ × تعداد خط
```

| تعداد خط | هزینه | هزینه بر خط |
|---|---|---|
| ۱ | ۰٫۷ | ۰٫۷۰ |
| ۵ | ۱٫۵ | ۰٫۳۰ |
| ۲۰ | ۴٫۵ | ۰٫۲۳ |

زمزمه از نظر **هزینه بر خط** گران‌ترین کار است. حالا نورون بین دو فایر تک‌خطی و یک فایر دوخطی، دومی را ترجیح می‌دهد.

این با شهود زیستی هم می‌خواند: هزینه‌ی اصلی اسپایک، دپلاریزاسیون خود سلول است نه تعداد پایانه‌ها.

### ۲٫۵ خودجوشی

نورون **در هیچ شرایطی مجبور به فایر نیست و در هیچ شرایطی از فایر منع نمی‌شود**. می‌تواند با صفر ورودی فایر کند و با ۱۰ ورودی ساکت بماند. تصمیم کاملاً از آنِ خودش است.

**نویز قابل تکرار:** هر نورون مولد شبه‌تصادفی بذردار خودش را دارد. سیستم هم خودجوش است، هم با همان بذر دقیقاً همان اجرا را تکرار می‌کند — بدون این، دیباگ کردن یک موجود ناهمگام غیرممکن است.

پارامتر **«دما»** در داشبورد شدت نویز را کنترل می‌کند: اول کار بالا (تا شبکه راه بیفتد)، بعد پایین.

---

## ۳. زمان

**ناهمگام، رویدادمحور، بدون هیچ ساعت سراسری.**

- تیک وجود ندارد. زمان یک عدد پیوسته است.
- هر نورون هر لحظه‌ای که خواست فایر می‌کند.
- هر یال **تأخیر ثابت خودش** را دارد: تصادفی بین ۱ تا ۲۰ میلی‌ثانیه‌ی مجازی.
- هیچ نورونی منتظر بقیه نمی‌ماند.

پیاده‌سازی: صف اولویت‌دار بر اساس زمان (شبیه‌سازی رویدادمحور گسسته).

**زمان مجازی، نه ساعت دیوار.** مدل با سرعت خودش پیش می‌رود و داشبورد یک ضریب سرعت دارد (کند/تند/توقف). بدون این، دیباگ ممکن نیست. «هر ۲ دقیقه» یعنی ۲ دقیقه‌ی درونیِ مدل.

---

## ۴. حلقه‌ها

- **حلقه‌های داخلی مغز آزادند** — A→B→C→A مجاز است. اینجا همان جایی است که «فکر» شکل می‌گیرد: فعالیتی که در خودش می‌چرخد و باقی می‌ماند.
- **حلقه‌ی مستقیم به خود (A→A) ممنوع است.** سیگنال نورون به خودش برنمی‌گردد.
- **حلقه‌ی بیرونی:** فقط خروجی لوب پایانی، از راه آینه‌ی دستگاه و با تأخیر، به نیمه‌ی ب لوب ورودی بازمی‌گردد. این‌طور مدل به مرور می‌فهمد دارد چه خروجی می‌دهد.

سیم‌کشی اولیه **تصادفیِ محلی‌گرا** است: هر نورون مختصاتی در فضای شبکه دارد و احتمال اتصال با فاصله افت می‌کند — انبوه یال‌های کوتاه به‌علاوه‌ی درصد کمی بزرگراه دوربرد. دلیلش در بخش ۱۲٫۳ آمده (بدون آن مقیاس چند-GPU ممکن نیست) و ضمناً به مغز واقعی نزدیک‌تر است.

---

## ۵. اقتصاد مانا

### ۵٫۱ قواعد پایه

- **هر ۵ سیگنال = ۱ مانا** (هر خط = ۰٫۲ مانا، با حساب عدد ثابت‌ممیز برای تکرارپذیری)
- **فقط فایرکننده هزینه می‌دهد.** گیرنده هیچ چیز نمی‌پردازد.
- نورون مانا را از **استخر لوب خودش** برمی‌دارد.
- مانا از بین نمی‌رود: می‌سوزد و بازمی‌گردد. سیستم بسته است.

### ۵٫۲ مانای در ترانزیت — منشأ کمبود واقعی

مانای سوخته **فوراً** برنمی‌گردد؛ بعد از حدود نیم‌ثانیه‌ی مجازی به استخر بازمی‌گردد.

اگر این نبود، فایر کردن در مجموع رایگان می‌شد و هیچ کمبودی وجود نداشت. با این تأخیر:
- شبکه‌ی پرهیاهو → مانا در ترانزیت گیر می‌کند → استخر خالی → گرسنگی
- شبکه‌ی آرام → همیشه پول هست

**این همان «درد» است: خستگی، نه فقر.** و خودش نرخ فایر را تنظیم می‌کند، بدون پارامتر دستی.

### ۵٫۳ استخر جداگانه برای هر لوب + نشت به عقب

هر لوب استخر مستقل دارد. **پاداش وارد استخر لوب پایانی می‌شود و به عقب نشت می‌کند:**

```
نمره‌ی انسان
     │
     ▼
[استخر لوب پایانی] ──۵۰٪──▶ [استخر لوب مرکزی] ──۵۰٪──▶ [استخر لوب ورودی]
```

تنبیه هم به همین ترتیب از انتها به عقب.

**چرا این مهم است:** بدون آن، نورونی که واقعاً کلمه‌ی درست را ساخت و نورونی که همان لحظه پرت‌وپلا می‌گفت، دقیقاً یک اندازه سود می‌بردند و هیچ‌وقت هیچ چیز یاد گرفته نمی‌شد. با نشت به عقب، **گرادیان اعتبار به‌شکل فیزیکی و بدون هیچ ریاضیات اضافه‌ای** ساخته می‌شود: هر چه به خروجیِ خوب نزدیک‌تر، سهم بیشتر.

#### ✅ رفع ضعف: گرسنگی لوب ورودی

لوب ورودی ته زنجیره‌ی نشت است و بدون چاره از گرسنگی می‌میرد. سه مهار همزمان:

**۱. درآمد پایه‌ی متناسب با جمعیت.** هر لوب در هر ثانیه‌ی مجازی حداقل درآمدی می‌گیرد که با تعداد نورون زنده‌اش متناسب است:

```
درآمد_پایه(لوب) = تعداد_نورون_زنده × نرخ_پایه
نرخ_پایه = ۰٫۵ مانا بر نورون بر ثانیه‌ی مجازی
```

این از خزانه‌ی سیستم می‌آید، نه از پاداش. تضمین می‌کند هیچ لوبی صرفاً به‌خاطر جایگاهش در زنجیره نمی‌میرد.

**۲. نشت دوطرفه.** پاداش فقط به عقب نمی‌رود؛ ۱۰٪ هم رو به جلو حرکت می‌کند. لوبی که ورودی خوبی تحویل داده، سهمی می‌گیرد حتی اگر خروجی نهایی را نساخته باشد.

**۳. نشت خودتنظیم.** نرخ نشت ثابت ۵۰٪ نیست؛ با نسبت گرسنگی تنظیم می‌شود:

```
نشت = ۰٫۵ × (۱ + گرسنگی_لوب_پایین‌دست)
گرسنگی = ۱ − (موجودی استخر ÷ ظرفیت هدف)
```

استخر گرسنه بیشتر می‌مکد، استخر سیر کمتر. این یک حلقه‌ی کنترل منفی است و از نوسان جلوگیری می‌کند.

### ۵٫۴ مالیات سکوت — ✅ حل شد

پیش‌تر «فعلاً ندارد» بود و ریسکش باز مانده بود. حالا حل می‌شود، ولی نه با مالیات ساده.

**مشکل:** اگر سکوت هزینه نداشته باشد، «هیچ‌کاری‌نکردن» یک تعادل پایدار است و شبکه منجمد می‌شود. ولی مالیات ثابت هم بد است: نورون تخصصی‌ای که فقط در موقعیت‌های نادر باید فایر کند، ناعادلانه می‌میرد.

**راه‌حل: هزینه‌ی زنده‌ماندنِ متناسب با ظرفیت.**

```
هزینه‌ی پایه در هر ثانیه‌ی مجازی = ۲٪ سقف مانا
  عادی   : ۰٫۴ مانا بر ثانیه
  حافظه‌ای: ۰٫۸ مانا بر ثانیه
  غول    : ۲٫۴ مانا بر ثانیه
```

چرا این کار می‌کند:
- **سکوت دیگر رایگان نیست** → انجماد کامل ناممکن می‌شود
- **ولی خیلی ارزان است** → نورون ساکت با یک فایر موفق در هر چند ثانیه زنده می‌ماند؛ تخصص‌گرایی ممکن می‌ماند
- **غول گران‌تر می‌سوزد** → مجبور است مفید باشد، نه فقط بزرگ

**خواب زمستانی:** نورونی که ۳۰ ثانیه‌ی مجازی هیچ ورودی و هیچ فایری نداشته، وارد حالت خواب می‌شود: هزینه‌ی پایه‌اش به ۱۰٪ می‌رسد ولی سرعت واکنشش هم کند می‌شود. با اولین ورودی بیدار می‌شود. این‌طور یک ذخیره‌ی خفته از نورون‌ها حفظ می‌شود به‌جای اینکه همه بمیرند.

### ۵٫۵ اعداد ذخیره

| رده | سقف مانا | هزینه‌ی فایر کامل | یعنی چند فایر کامل |
|---|---|---|---|
| عادی | ۲۰ | ۴ | ۵ |
| حافظه‌ای | ۴۰ | ۸ | ۵ |
| غول | ۱۲۰ | ۱۲ | ۱۰ |

غول عمداً بیشتر، چون باید بتواند برای «تحقیق» پس‌انداز کند.

---

## ۶. مرگ

| مرحله | شرط | رفتار |
|---|---|---|
| **سلامت** | مانا > ۲۰٪ ظرفیت | عادی |
| **ایگنور** | مانا < ۲۰٪ | ورودی می‌گیرد، فایر نمی‌کند |
| **اسپم** | مانا صفر و ۱۰ ثانیه بی‌درآمد | ورودی‌ها را قطع می‌کند و به خروجی‌ها اسپم می‌دهد — به مدت ۲ ثانیه |
| **حذف** | پایان فاز اسپم | برای همیشه حذف |

**نورون‌ها می‌توانند به هم بگویند چقدر مانا دارند.**

### ✅ رفع ضعف: موج مرگ زنجیره‌ای

این خطرناک‌ترین ضعف باقی‌مانده بود. حلقه‌ی تشدید: نورون می‌میرد → اسپم می‌دهد → همسایه‌ها بار می‌گیرند → استخر خالی → همسایه‌ها می‌میرند → اسپم بیشتر → **فروپاشی کل شبکه در چند ثانیه**.

در یک شبکه‌ی بازگشتی این تقریباً حتمی است. چهار مهار مستقل:

**۱. اسپم مانا مصرف می‌کند و از سهم خودش برداشته می‌شود.** نورون در حال مرگ از یک اعتبار پایانی ثابت (۵ مانا) اسپم می‌زند، نه از استخر. وقتی تمام شد، اسپم قطع می‌شود حتی اگر ۲ ثانیه نگذشته باشد. **اسپم دیگر نمی‌تواند استخر را خالی کند.**

**۲. گیرنده هزینه‌ای نمی‌دهد** (قاعده‌ی موجود ۵٫۱). پس اسپم مستقیماً همسایه را فقیر نمی‌کند؛ فقط ورودی بی‌معنی می‌فرستد. این نیمی از حلقه را از پیش می‌شکند.

**۳. قرنطینه‌ی خودکار.** نورونی که در پنجره‌ی اخیر بیش از ۸۰٪ ورودی‌هایش از منابع در حال مرگ آمده باشد، آن خطوط را موقتاً می‌بندد. اطلاعات این را از همان قابلیت «نورون‌ها می‌توانند مانای هم را ببینند» می‌گیرد — بدون هیچ سازوکار جدیدی.

**۴. سقف نرخ مرگ در هر لوب.** در هر ثانیه‌ی مجازی حداکثر **۰٫۱٪** جمعیت هر لوب می‌تواند حذف شود. اگر بیشتر واجد شرایط مرگ باشند، در صف می‌مانند و درآمد پایه فرصت نجاتشان را می‌دهد. **این آخرین سد در برابر فروپاشی است.**

```
اگر نرخ مرگ لوب > ۰٫۱٪ در ثانیه:
    → حالت اضطراری لوب
    → درآمد پایه ×۳
    → حذف‌ها تا عبور از بحران معلق می‌ماند
```

**نتیجه:** موج مرگ می‌تواند شروع شود (که مطلوب است — فشار تکاملی)، ولی نمی‌تواند از کنترل خارج شود.

---

## ۷. حافظه و تابع ذخیره‌ساز

### ۷٫۱ اختیارات

| چه کسی | روی خودش | روی زیردست |
|---|---|---|
| **عادی** | — (فاز ۱) | — |
| **حافظه‌ای** | فقط **پارامترهای** تابع ذخیره‌ساز | — |
| **غول** | **بازنویسی کامل، رایگان، نامحدود** | **بازنویسی کامل** — هر ۲ دقیقه یک بار، **هزینه‌ی سنگین مانا** |

غول تابع ذخیره‌ساز زیردستانش را **می‌بیند** و می‌تواند کاملاً عوض کند.

### ۷٫۲ بازنویسی حافظه پس از تغییر تابع

بعد از هر تغییر تابع ذخیره‌ساز، **کل حافظه‌ی نورون با تابع جدید بازنویسی می‌شود.**

**تابع ذخیره‌ساز دوطرفه است.** ✅ *(تصمیم‌گیری شد)*

هر تابع ذخیره‌ساز دو نیمه دارد:

```
نوشتن:   معنا  ──رمزگذاری──▶  بایت‌های حافظه
خواندن:  بایت‌های حافظه  ──رمزگشایی──▶  معنا
```

مراسم تعویض تابع:

```
۱. با تابع قدیم کل حافظه رمزگشایی می‌شود      → محتوای معنایی
۲. تابع جدید جایگزین می‌شود
۳. با تابع جدید دوباره رمزگذاری می‌شود        → بایت‌های جدید
۴. اگر مرحله‌ی ۱ یا ۳ خطا داد یا سوخت تمام شد → نورون به خواب می‌رود
```

**پیامدها:**
- محتوای حافظه در تعویض تابع **حفظ می‌شود** — غول می‌تواند فشرده‌سازی بهتری تحمیل کند بدون اینکه خاطرات زیردست پاک شود.
- اگر تابع جدید ظرفیت کمتری داشته باشد، بخشی از محتوا **جا نمی‌شود**. قاعده: هر چه دیرتر نوشته شده، زودتر دور ریخته می‌شود (فراموشی از قدیمی‌ها).
- **خطر:** غول می‌تواند تابعی بنویسد که رمزگشایی‌اش با رمزگذاری‌اش نخواند (دوطرفگی شکسته). آزمون اجباری پیش از اعمال: چند نمونه رمزگذاری → رمزگشایی شود؛ اگر برابر نبود، تعویض رد می‌شود و هزینه‌ی مانا **سوخته** است. این خودش فشار تکاملی روی غول‌ها می‌گذارد که توابع سالم بنویسند.

### ۷٫۳ چرا غول‌ها باید در حال تحقیق بمانند

تنش واقعی: اگر تغییر زیردست خیلی گران باشد، غول عاقل هرگز ریسک نمی‌کند و **منجمد می‌شود**.

راه‌حل: **فکر کردن ارزان، اعمال کردن گران.**
- آزمایش روی خودش: رایگان و نامحدود (آزمایشگاه شخصی)
- نشان دادن نتیجه به غول دیگر: هزینه‌ی کم (شبکه‌ی دوم)
- تحمیل به زیردست: خیلی گران

**بازگشت سرمایه:** اگر غول تابع زیردستش را عوض کرد و بعد آن خوشه عملکرد بهتری داشت (استخر لوب رشد کرد)، غول پاداش می‌گیرد. بدون این، تحقیق فقط هزینه است و غول‌ها به‌سرعت محافظه‌کار می‌شوند — و کل ایده‌ی «تکامل توابع فراتر از تصور ما» می‌میرد.

---

## ۸. شبکه‌ی دوم — کانال غیربیتی غول‌ها

بیت برای تبادل تابع و نتیجه کافی نیست. پس دو شبکه داریم:

| | شبکه‌ی اول | شبکه‌ی دوم |
|---|---|---|
| **بین** | همه | فقط غول‌ها |
| **محتوا** | بیت | بسته: بایت‌کد، بلوک داده |
| **ترافیک** | زیاد | کم |
| **هزینه** | ارزان | گران |
| **تشبیه** | سیناپس | نورومدولاسیون / هورمون |

هزینه‌ی گران عمدی است، وگرنه غول‌ها بی‌وقفه با هم حرف می‌زنند.

---

## ۹. ماشین مجازی

توابع نورون‌ها **کد بومی نیستند، بایت‌کدند** و یک ماشین مجازی کوچک اجرایشان می‌کند.

**چرا این اجباری است:**
- try/catch حلقه‌ی بی‌نهایت را نمی‌گیرد — و وقتی غول‌ها آزادانه تابع می‌نویسند، حلقه‌ی بی‌نهایت **بسیار محتمل‌تر از کرش** است. یک تابع بدنوشته کل نخ را قفل می‌کند و هیچ استثنایی پرتاب نمی‌شود.
- **بایت‌کد داده است.** غول می‌تواند بخواند، تحلیل کند، جهش بدهد، بنویسد — چون فقط یک آرایه‌ی بایت است. با کد بومی این غیرممکن بود. **خواسته‌ی «غول‌ها توابع را تکامل بدهند» فقط با ماشین مجازی شدنی است.**

**تضمین‌ها:**

| خطر | مهار |
|---|---|
| حلقه‌ی بی‌نهایت | **سوخت** — سقف تعداد دستور در هر اجرا |
| دسترسی خارج از محدوده | حافظه محدود به همان ۳۲ب / ۱ک / ۴ک خودش |
| تقسیم بر صفر، دستور نامعتبر | کد خطا، نه استثنا |

**رفتار در خطا:** تابعی که سوختش تمام شود یا خطا بدهد → **نورون به خواب می‌رود**. غول بالادست باید سریع فکری برایش بکند، وگرنه زیردست می‌میرد.

> این یک فشار تکاملی واقعی روی غول‌ها می‌گذارد و یکی از زیباترین بخش‌های طراحی است.

**توابع اولیه را ما می‌نویسیم؛ از آن به بعد غول‌ها آن‌قدر تکاملشان می‌دهند که از چیزی که تا حالا دیده‌ایم فراتر برود.**

### ۹٫۱ مجموعه دستورات — ✅ طراحی شد

**ثبات‌ها:** ۱۶ ثبات ۳۲ بیتی. بدون پشته (پشته یعنی دسترسی غیرقابل پیش‌بینی و روی GPU سم است).

**قالب دستور:** ۴ بایت ثابت — `[کد عمل: ۸ بیت][مقصد: ۴][عملوند ۱: ۴][عملوند ۲/فوری: ۱۶]`

| گروه | دستورها | توده (خطی) | حافظه‌ای | غول |
|---|---|---|---|---|
| حساب | `ADD SUB MUL DIV MOD` | ✅ | ✅ | ✅ |
| منطق | `AND OR XOR NOT SHL SHR` | ✅ | ✅ | ✅ |
| مقایسه | `EQ LT GT` (نتیجه ۰/۱ در ثبات) | ✅ | ✅ | ✅ |
| انتخاب | `SEL d,c,a,b` → `d = c ? a : b` | ✅ | ✅ | ✅ |
| حافظه | `LD ST` (محدود به حافظه‌ی خود) | ✅ | ✅ | ✅ |
| حسگر | `SENSE` (ورودی، تازگی، مانا، زمان، نویز) | ✅ | ✅ | ✅ |
| کنش | `FIRE mask,bits` · `SLEEP` | ✅ | ✅ | ✅ |
| حلقه | `LOOP n` (باز شده، سقف ثابت) | ❌ | ✅ (۸) | ✅ (۶۴) |
| پرش | `JMP JZ JNZ CALL RET` | ❌ | ❌ | ✅ |
| فراشناخت | `PEEK POKE` (دیدن/نوشتن تابع زیردست) | ❌ | ❌ | ✅ |
| ارتباط | `SEND RECV` (شبکه‌ی دوم) | ❌ | ❌ | ✅ |

**`SEL` ستون فقرات نسخه‌ی خطی است** — هر منطق شرطی با آن ساخته می‌شود، بدون انشعاب واقعی.

**سوخت:**

| رده | سقف دستور در هر اجرا |
|---|---|
| عادی | ۶۴ |
| حافظه‌ای | ۵۱۲ |
| غول | ۸٬۱۹۲ |

**تضمین ایمنی جهش:** هر توالی بایت یک برنامه‌ی معتبر است. کد عمل نامعتبر = `NOP`، ثبات خارج از محدوده = پیمانه‌ی ۱۶، آدرس حافظه‌ی خارج از محدوده = پیمانه‌ی اندازه. **یعنی جهش هرگز نمی‌تواند برنامه را خراب کند، فقط می‌تواند بی‌فایده‌اش کند** — شرط لازم برای تکامل واقعی.

### ۹٫۲ هزینه‌ی غول برای تغییر زیردست — ✅ فرمول

```
هزینه = پایه × ضریب_تفاوت × ضریب_تکرار

پایه          = ۴۰ مانا
ضریب_تفاوت    = ۱ + (فاصله‌ی همینگ بایت‌کد جدید و قدیم ÷ طول)   → ۱ تا ۲
ضریب_تکرار    = ۲ ^ (تعداد تغییرات این زیردست در ۱۰ دقیقه‌ی اخیر)
```

با سقف مانای غول (۱۲۰):
- تغییر کوچک روی زیردست دست‌نخورده: ~۴۰ مانا → شدنی
- بازنویسی کامل: ~۸۰ مانا → گران ولی ممکن
- دومین تغییر روی همان زیردست در ۱۰ دقیقه: ~۱۶۰ مانا → **بیش از سقف، غیرممکن**

قید «هر ۲ دقیقه یک بار» به‌جای خود باقی است؛ این فرمول علاوه بر آن، **دستکاری وسواسی** را مهار می‌کند و غول را مجبور می‌کند بین تغییرات صبر کند و نتیجه را ببیند.

**بازگشت سرمایه:** اگر تا ۵ دقیقه پس از تغییر، اعتبار متوسط زیردستان آن غول رشد کند، غول **دو برابر هزینه** را پس می‌گیرد. بدون این، تحقیق فقط ضرر است و غول‌ها منجمد می‌شوند.

---

## ۱۰. مانای طلایی

| قابلیت | شرح |
|---|---|
| افزایش مسیر | تعداد مسیرهایش ۱۰٪ بیشتر |
| تغییر مسیر رایگان | ۴ تغییر یکجا (با مانای معمولی: یکی‌یکی و پولی) |
| جوانه‌زدن | تقسیم سلولی — **فقط با طلا** |

**جوانه‌زدن به فازهای بسیار آینده موکول شده است.**

### ✅ رفع ضعف: «لوب مرکزی تصمیم می‌گیرد» یعنی چه؟

این جمله یک تناقض پنهان داشت: لوب مرکزی یک موجودیت واحد نیست که بتواند تصمیم بگیرد — فقط انبوهی نورون محلی است. یک «مدیر مرکزی» هم با اصل حاکم معماری (نبود ناظر مرکزی) می‌جنگد.

**راه‌حل: مزایده، نه مدیر.**

طلا وارد یک صندوق در لوب مرکزی می‌شود و به مزایده گذاشته می‌شود:

```
۱. هر غول می‌تواند برای خودش یا یکی از زیردستانش پیشنهاد بدهد
۲. پیشنهاد = (نامزد، قابلیت درخواستی، مبلغ مانای معمولی که حاضر است بسوزاند)
۳. برنده: بالاترین حاصل‌ضرب  مبلغ پیشنهادی × اعتبار اخیر نامزد
۴. مانای پیشنهادی برنده سوخته می‌شود، طلا به نامزد می‌رسد
```

«اعتبار اخیر» همان شمارنده‌ی محوشونده‌ای است که در ۵٫۳ برای توزیع پاداش استفاده می‌شود — سازوکار جدیدی لازم نیست.

چرا این درست است:
- **کاملاً محلی و غیرمتمرکز** — هیچ ناظری وجود ندارد
- **پرداخت واقعی** → غول برای نامزد بی‌ارزش پیشنهاد نمی‌دهد
- **ضرب در اعتبار** → پول‌داشتن به‌تنهایی کافی نیست؛ نامزد باید واقعاً مفید بوده باشد
- **خودتنظیم** → وقتی طلا کمیاب است رقابت بالا می‌رود

---

## ۱۱. دستگاه

### ۱۱٫۱ کدک فارسی

```
انسان → متن فارسی → نرمال‌سازی → UTF-8 → بیت → نیمه‌ی الف لوب ورودی
لوب پایانی → بیت → بایت → UTF-8 → کلمه → نمایش به انسان
                                    └──▶ آینه ──تأخیر──▶ نیمه‌ی ب لوب ورودی
```

- **فاصله جداکننده‌ی کلمات است.** فاصله به کلمه‌ی قبلش می‌چسبد و همان لحظه نمره می‌گیرد.
- نرمال‌سازی: یکسان‌سازی ی/ک عربی و فارسی، نیم‌فاصله، ارقام.
- بایت‌های نامعتبر UTF-8 دور ریخته می‌شوند (و در فاز ۱ می‌توانند تنبیه کوچک بگیرند).

### ۱۱٫۲ نمره‌دهی

- بازه‌ی **نامحدود**: منفی هزار تا مثبت هرچه بخواهی.
- ولی قبل از اعمال از یک **فشرده‌ساز** رد می‌شود (`sign(s)·log(1+|s|)·k`) و یک **کف حیاتی** وجود دارد که هیچ تنبیهی نتواند کل مغز را یک‌جا بکشد.

> بدون فشرده‌ساز، یک بار عصبانی شدن = مرگ کل موجود، از جمله نورون‌هایی که در آن خروجی هیچ نقشی نداشتند.

### ۱۱٫۳ برنامه‌ی درسی

| فاز | روش | منبع سیگنال | حجم سیگنال |
|---|---|---|---|
| ۱ | نمره‌دهی دستی کلمه‌به‌کلمه | انسان | ~۶۰ در دقیقه |
| ۲ | دیکشنری بسیار کامل فارسی | خودکار | زیاد |
| ۳ | **تشخیص ترتیب** — جمله‌ی درست در برابر به‌هم‌ریخته | خودکار | زیاد |
| ۴ | خواندن انبوه متن | خودکار | بسیار زیاد |
| ۵ | پیش‌بینی کلمه‌ی حذف‌شده، ۵٪ → ۳۰–۴۰٪ | خودکار | بسیار زیاد |

**دو نکته‌ی حیاتی:**

۱. **فاز ۳ (ترتیب) اضافه شده** — بین «کدام کلمه معنا دارد» و «معنای جمله» یک پرش بزرگ بود. مدل باید اول یاد بگیرد «کتاب را خواندم» درست است و «را خواندم کتاب» نه.

۲. **از فاز ۲ به بعد پهنای باند معلم دیگر مشکل نیست**، چون جواب درست را خودِ داده دارد. میلیون‌ها سیگنال در ساعت ممکن می‌شود. **فاز ۱ را باید تا حد ممکن کوتاه کرد** — با نمره‌دهی دستی هرگز به فارسی روان نمی‌رسیم.

### ۱۱٫۴ داشبورد

- نقشه‌ی حرارتی مانا به تفکیک لوب
- نرخ فایر، نرخ مرگ، تعداد نورون‌های خواب
- شنود زنده‌ی خروجی + ورودی نمره
- کنترل سرعت زمان مجازی و دمای نویز
- **دکمه‌ی خاموش کردن:** مدل را متوقف می‌کند، حافظه‌ی تمام نورون‌ها را نگه می‌دارد و روی دیسک می‌نویسد
- بارگذاری از چک‌پوینت

---

## ۱۲. پیاده‌سازی و مقیاس‌پذیری

**زبان: C++** — کنترل کامل حافظه، بدون garbage collector، صف رویداد داغ، آرایه‌های فشرده. (Rust کنار گذاشته شد: borrow checker با گرافی که نورون‌ها همدیگر را تغییر می‌دهند مدام سر راه است.)

> **بازنگری مهم:** در پیش‌نویس اول نوشته بودم «GPU عملاً کنار می‌رود». **آن حکم پس گرفته شد.** با سه تغییر معماری زیر، مقیاس روی ده‌ها GPU و CPU ممکن است — و بدون مقیاس، سقف هوش مدل قفل می‌شود.

### ۱۲٫۱ کلید ماجرا: حداقل تأخیر یال = پنجره‌ی موازی رایگان

هر یال تأخیری بین **۱ تا ۲۰ میلی‌ثانیه‌ی مجازی** دارد. یعنی حداقل تأخیر شبکه `Δ = ۱ms` است.

از این یک قضیه‌ی ساده و بسیار قدرتمند بیرون می‌آید:

> **هیچ رویدادی در بازه‌ی `[t, t+Δ)` نمی‌تواند روی رویداد دیگری در همان بازه اثر بگذارد** — چون سیگنالش زودتر از `t+Δ` به هیچ‌جا نمی‌رسد.

پس تمام رویدادهای داخل هر پنجره‌ی ۱ میلی‌ثانیه‌ای **کاملاً مستقل‌اند و می‌توان همه را همزمان پردازش کرد**.

```
زمان مجازی ──────────────────────────────────▶
   │ پنجره ۱ │ پنجره ۲ │ پنجره ۳ │ پنجره ۴ │
   │ ۲.۱M    │ ۱.۹M    │ ۲.۳M    │ ۲.۰M    │  ← رویداد مستقل
   │ رویداد  │ رویداد  │ رویداد  │ رویداد  │
   └─موازی──┘└─موازی──┘└─موازی──┘└─موازی──┘
       ▲          ▲
       └──────────┴── فقط بین پنجره‌ها همگام‌سازی لازم است
```

**این تیک نیست.** نورون‌ها هنوز هر لحظه‌ای که بخواهند فایر می‌کنند، زمان‌ها هنوز اعداد پیوسته‌اند، هیچ‌کس با بقیه قدم برنمی‌دارد. فقط **اجرا** دسته‌ای می‌شود، نه **معنا**. صف اولویت‌دار سراسری — که گلوگاه واقعی بود — حذف می‌شود.

با ۱۰⁹ نورون، هر پنجره میلیون‌ها رویداد مستقل دارد: دقیقاً بار کاری‌ای که GPU برایش ساخته شده.

### ۱۲٫۲ بایت‌کد خطی برای توده

مشکل GPU بایت‌کد نیست، **انشعاب** است. راه‌حل: دو گونه اجرا.

| | نورون عادی (۹۷٪) | حافظه‌ای و غول (۳٪) |
|---|---|---|
| **کجا اجرا می‌شود** | GPU | CPU |
| **نوع بایت‌کد** | خطی، طول‌ثابت | کامل، با `if` و حلقه |
| **شرط‌ها** | با ماسک (`if` ندارد) | واقعی |
| **سوخت** | ثابت و یکسان برای همه | متغیر، با سقف |
| **واگرایی warp** | صفر | بی‌اهمیت (کم‌تعدادند) |

همه‌ی لِین‌های یک warp همیشه همان N دستور را اجرا می‌کنند؛ به‌جای پرش، نتیجه‌ی شاخه‌ها با ماسک ترکیب می‌شود. نتیجه: SIMD کامل.

**و همچنان بایت‌کد است، همچنان داده است، همچنان غول‌ها می‌توانند تکاملش بدهند.** فقط `if` و حلقه‌ی درونی در دسترس توده نیست.

> ### ⚠️ رفع یک ابهام واژگانی
>
> «خطی بودن» یا «بدون انشعاب بودن» **هیچ ربطی به یال‌ها و اتصالات ندارد.** دو مفهوم کاملاً جدا:
>
> | | چیست | وضعیت |
> |---|---|---|
> | **یال / اتصال** | سیم بین دو نورون | ✅ کاملاً دست‌نخورده — اتصال دوربرد و بین‌کارتی هر دو هستند و لازم‌اند |
> | **انشعاب** | دستور `if` و حلقه **درون** برنامه‌ی یک نورون | ⚠️ فقط این، آن هم فقط برای توده، محدود می‌شود |
>
> **نورون همچنان کاملاً تصمیم‌گیرنده است:** خودش انتخاب می‌کند فایر کند یا نه، روی کدام خطوط، چه بیتی، حافظه‌اش را عوض کند یا نه. فقط **شکل نوشتن** برنامه‌اش فرق می‌کند، نه **اختیارش**.
>
> به‌جای:
> ```
> اگر ورودی ۳ روشن بود: برو خط ۲۰
> وگرنه: ۱۰ بار حلقه بزن
> ```
> نوشته می‌شود:
> ```
> نتیجه‌ی الف = محاسبه‌ی مسیر اول
> نتیجه‌ی ب  = محاسبه‌ی مسیر دوم
> خروجی      = انتخاب(شرط، الف، ب)
> ```
> هر دو مسیر حساب می‌شوند و بعد یکی انتخاب می‌شود. از نظر منطقی همان کار انجام می‌شود، ولی تعداد دستورها همیشه ثابت است — پس ۳۲ نورون در یک warp دقیقاً هم‌قدم می‌مانند.

> **تقسیم کار طبیعی: توده روی GPU فکر می‌کند، نخبه‌ها روی CPU.**
> این با معماری هم‌خوان است — غول‌ها کم‌تعداد و کارشان پیچیده و ترتیبی است؛ دقیقاً همان چیزی که CPU در آن خوب است.

#### ⚠️ یک مشکل عددی که در مقیاس بزرگ ظاهر می‌شود

اگر **همه‌ی** نورون‌های حافظه‌ای روی CPU بروند، با رشد شبکه از دست خارج می‌شود:

| کل نورون | حافظه‌ای (CPU؟) | غول (CPU) |
|---|---|---|
| ۱۰⁵ | ۳٬۰۰۰ | ۱۵۰ |
| ۱۰⁷ | ۳۰۰٬۰۰۰ | ۱۵٬۰۰۰ |
| ۱۰⁹ | **۳۰٬۰۰۰٬۰۰۰** ❌ | ۱٬۵۰۰٬۰۰۰ |

سی میلیون نورون حافظه‌ای روی CPU غیرممکن است — CPU خودش گلوگاه می‌شود و کل مزیت GPU از بین می‌رود.

**راه‌حل: تقسیم سه‌طرفه به‌جای دوطرفه.**

| رده | محل اجرا | چرا |
|---|---|---|
| عادی (۹۷٪) | GPU، بایت‌کد خطی | توده‌ی عظیم، کار ساده |
| **حافظه‌ای (۳٪)** | **GPU، بایت‌کد خطی + سوخت بیشتر و حافظه‌ی بزرگ‌تر** | هنوز خیلی زیادند؛ کارشان پیچیده‌تر است ولی نه آن‌قدر که به `if` نیاز باشد |
| غول (۰٫۱۵٪) | CPU، بایت‌کد کامل | واقعاً کم‌تعداد، کار واقعاً پیچیده و ترتیبی |

با این تقسیم، در ۱۰⁹ نورون فقط ۱٫۵ میلیون غول روی CPU می‌ماند — که با چند ده هسته شدنی است.

**✅ تصمیم نهایی: حافظه‌ای‌ها روی GPU، با بایت‌کد نیمه‌خطی.**

نگرانی این بود که نورون حافظه‌ای بدون `if` نتواند کار مفیدی بکند. راه‌حل بدون بازگشت به CPU:

**حلقه‌ی باز شده با سقف ثابت.** نورون حافظه‌ای اجازه‌ی حلقه دارد، ولی حلقه‌ای که در زمان کامپایل باز می‌شود و سقف تکرار ثابت دارد (مثلاً ۸ بار). همه‌ی لِین‌ها همان ۸ تکرار را اجرا می‌کنند؛ آن‌که زودتر تمام کرده، بقیه را با ماسک بی‌اثر می‌گذراند.

```
هزینه: همیشه ۸ تکرار محاسبه می‌شود (اتلاف دارد)
سود  : واگرایی صفر، روی GPU می‌ماند
```

با ۳٪ جمعیت، این اتلاف قابل تحمل است و در عوض ۳۰ میلیون نورون از CPU برداشته می‌شود.

**جدول نهایی محل اجرا:**

| رده | جمعیت در ۱۰⁹ | محل | بایت‌کد |
|---|---|---|---|
| عادی | ۹۶۸٫۵ میلیون | GPU | خطی محض |
| حافظه‌ای | ۳۰ میلیون | GPU | نیمه‌خطی (حلقه‌ی باز شده) |
| غول | ۱٫۵ میلیون | CPU | کامل |

#### ✅ رفع ضعف: غول‌های دو ناحیه‌ی مختلف

سؤال باز بود که غول‌های روی دو کارت مختلف چطور حافظه‌ی هم را ببینند. راه‌حل: **غول‌ها اصلاً روی کارت نیستند.**

همه‌ی غول‌ها در **حافظه‌ی اصلی میزبان** زندگی می‌کنند، نه در VRAM:

| کل نورون | تعداد غول | RAM لازم |
|---|---|---|
| ۱۰⁷ | ۱۵٬۰۰۰ | ۰٫۰۶ GB |
| ۱۰⁹ | ۱٫۵ میلیون | ۶٫۱ GB |
| ۱۰¹⁰ | ۱۵ میلیون | ۶۱ GB |

پس شبکه‌ی دوم (کانال غیربیتی غول‌ها) کاملاً در فضای آدرس مشترک میزبان اجرا می‌شود — **هیچ ارتباط بین‌کارتی‌ای لازم ندارد.** فقط سیگنال‌های بیتی بین غول و زیردستانش از مرز عبور می‌کنند، که ترافیک ناچیزی است.

در مقیاس چندماشینی، غول‌ها به گروه‌های محلی تقسیم می‌شوند و ارتباط بین گروه‌ها از شبکه می‌گذرد — ولی چون شبکه‌ی دوم عمداً کم‌ترافیک و گران است، مشکلی ایجاد نمی‌کند.

### ۱۲٫۳ ⚠️ سیم‌کشی محلی‌گرا — تغییر نسبت به تصمیم قبلی

پیش‌تر قرار شد سیم‌کشی اولیه **کاملاً تصادفی** باشد. این با مقیاس چند-GPU **ناسازگار** است:

- سیم‌کشی کاملاً تصادفی روی ۱۰ کارت → **~۹۰٪ یال‌ها از مرز کارت رد می‌شوند** → صدها مگابایت ترافیک در هر پنجره → اتصال بین کارت‌ها خفه می‌شود و ۱۰ کارت از ۱ کارت کندتر کار می‌کند.

**جایگزین: تصادفیِ محلی‌گرا.** هر نورون یک مختصات در فضای شبکه دارد. احتمال اتصال با فاصله افت می‌کند:

- ~۹۰٪ یال‌ها: همسایگی نزدیک (داخل همان کارت)
- ~۱۰٪ یال‌ها: بزرگراه‌های دوربرد (بین کارت‌ها)

نتیجه: عبور از مرز به **~۵٪** می‌رسد و ترافیک بین‌کارتی صدها برابر کم می‌شود.

> این اتفاقاً به مغز واقعی **نزدیک‌تر** هم هست: شبکه‌ی جهان‌کوچک — انبوه اتصالات محلی به‌علاوه‌ی چند بزرگراه بلند. پس هم لازم است، هم درست‌تر.

### ۱۲٫۴ سلسله‌مراتب اجرا

```
        ┌─────────────────────────────────────────┐
        │              هماهنگ‌کننده                │
        │   مرز پنجره · مانا · چک‌پوینت · دستگاه   │
        └────────────────┬────────────────────────┘
                         │
      ┌──────────────────┼──────────────────┐
      ▼                  ▼                  ▼
  ┌────────┐        ┌────────┐        ┌────────┐
  │ ناحیه ۰│        │ ناحیه ۱│  ···   │ ناحیه N│
  │ GPU 0  │        │ GPU 1  │        │ GPU N  │
  │        │        │        │        │        │
  │ توده   │        │ توده   │        │ توده   │
  └───┬────┘        └───┬────┘        └───┬────┘
      │                 │                 │
  ┌───▼────┐        ┌───▼────┐        ┌───▼────┐
  │ نخ CPU │        │ نخ CPU │        │ نخ CPU │
  │ نخبه‌ها│        │ نخبه‌ها│        │ نخبه‌ها│
  └────────┘        └────────┘        └────────┘
```

#### ✅ رفع ضعف: نگاشت لوب‌ها به ناحیه‌ها

سؤال باز بود که هر لوب یک کارت بگیرد یا هر ناحیه تکه‌ای از هر سه لوب باشد.

**تصمیم: هر ناحیه تکه‌ای از هر سه لوب.**

اگر هر لوب یک کارت جدا می‌گرفت:
- ❌ **هر سیگنال بین‌لوبی از مرز کارت رد می‌شد** — و جریان اصلی مغز دقیقاً همین است (ورودی → مرکزی → پایانی)
- ❌ کارت لوب مرکزی اشباع و بقیه بی‌کار می‌ماندند (لوب مرکزی بسیار بزرگ‌تر است)
- ❌ مرگ گسترده در یک لوب = یک کارت خالی و غیرقابل توازن

با تکه‌بندی عمودی:
- ✅ مسیر ورودی→مرکزی→پایانی برای نورون‌های هم‌مختصات **داخل همان کارت** می‌ماند
- ✅ بار به‌طور طبیعی متوازن است
- ✅ سیم‌کشی محلی‌گرا (۱۲٫۳) دقیقاً همین را تقویت می‌کند: مختصات نورون تعیین‌کننده است، نه لوبش

```
        کارت ۰        کارت ۱        کارت ۲
      ┌─────────┐   ┌─────────┐   ┌─────────┐
ورودی │ ▓▓▓     │   │ ▓▓▓     │   │ ▓▓▓     │
مرکزی │ ▓▓▓▓▓▓▓ │   │ ▓▓▓▓▓▓▓ │   │ ▓▓▓▓▓▓▓ │
پایانی│ ▓▓      │   │ ▓▓      │   │ ▓▓      │
      └─────────┘   └─────────┘   └─────────┘
         جریان عمودی داخل کارت می‌ماند
```

استخر هر لوب همچنان منطقاً یکی است؛ فقط زیرحساب‌های محلی دارد (۱۲٫۵).

**چرخه‌ی هر پنجره:**

```
۱. هر GPU رویدادهای پنجره‌ی خودش را دسته‌ای اجرا می‌کند   ← موازی کامل
۲. هر CPU نخبه‌های ناحیه‌ی خودش را اجرا می‌کند             ← همزمان با ۱
۳. سیگنال‌های عبوری از مرز بسته‌بندی و ارسال می‌شوند       ← فشرده، ~۵٪
۴. تسویه‌ی مانا و همگام‌سازی سبک                          ← تنها نقطه‌ی همگامی
```

مرحله‌ی ۳ **همپوشان** با پردازش پنجره‌ی بعدی انجام می‌شود (چون حداقل تأخیر ۱ms است، سیگنال فرصت دارد برسد). یعنی ارتباط پشت محاسبه پنهان می‌شود.

### ۱۲٫۵ مانا در مقیاس

استخر مشترک با ۱۰ کارت به یک گلوگاه اتمیک تبدیل می‌شود. راه‌حل استاندارد:

- هر ناحیه یک **زیرحساب محلی** از استخر لوب دارد
- نورون‌ها فقط با زیرحساب محلی معامله می‌کنند (بدون قفل سراسری)
- در مرز هر پنجره، زیرحساب‌ها با استخر مرکزی **تسویه** می‌شوند

از نظر رفتاری تفاوت محسوسی ندارد، ولی قفل سراسری حذف می‌شود.

### ۱۲٫۶ سقف واقع‌بینانه

| پیکربندی | نورون | یادداشت |
|---|---|---|
| ۱ CPU چندهسته‌ای | ۱۰⁵ – ۱۰⁶ | مرحله‌ی نمونه |
| ۱ GPU + CPU | ۱۰⁷ – ۱۰⁸ | یک ورک‌استیشن |
| ۱۰ GPU + CPU | ۱۰⁹ | نیازمند سیم‌کشی محلی‌گرا |
| خوشه‌ی چندماشینی | ۱۰¹⁰+ | همان معماری، ناحیه‌ها روی نودها |

#### ✅ رفع ضعف: بودجه‌ی VRAM

برآورد اولیه‌ی «۱۰۰ بایت بر نورون» خوش‌بینانه بود. حساب واقعی:

| جزء | ساده‌لوحانه | بهینه‌شده |
|---|---|---|
| حافظه‌ی نورون | ۳۲ B | ۳۲ B |
| بایت‌کد تابع | ۲۵۶ B (درون‌خطی) | **۴ B** (شناسه‌ی مشترک) |
| ۲۰ یال | ۱۰۰ B | ۱۰۰ B |
| وضعیت (مانا، زمان، بذر) | ۱۰ B | ۱۰ B |
| **جمع** | **۳۹۸ B** | **۱۴۶ B** |
| **۱۰⁹ نورون** | **۳۹۸ GB** ❌ | **۱۴۶ GB** ✅ |

**کلید بهینه‌سازی: بایت‌کد مشترک.** میلیون‌ها نورون یک تابع یکسان دارند. به‌جای نگهداری کپی در هر نورون، یک **مخزن توابع** نگه می‌داریم و هر نورون فقط یک شناسه‌ی ۴ بایتی دارد.

مزایای جانبی:
- warp با شناسه‌ی یکسان مرتب می‌شود → هم‌گرایی کامل اجرا
- غول که تابعی را تغییر می‌دهد، در واقع شناسه‌ی زیردست را به تابع جدید تغییر می‌دهد → عملیات ارزان
- توابع پرطرفدار در کش می‌مانند

با ۱۴۶ گیگابایت روی ۱۰ کارت = **۱۴٫۶ گیگابایت بر کارت** — روی کارت‌های ۲۴ گیگابایتی جا می‌شود.

#### ✅ رفع ضعف: بودجه‌ی پهنای باند

محاسبه‌ی ترافیک بین‌کارتی در ۱۰⁹ نورون (میانگین ۴ خط در هر فایر، ۸ بایت بر سیگنال):

| نرخ فایر | سیم‌کشی تصادفی (۹۰٪ عبور) | سیم‌کشی محلی‌گرا (۵٪ عبور) |
|---|---|---|
| ۱ Hz | ۲۸٫۸ GB/s ✅ | ۱٫۶ GB/s ✅ |
| ۱۰ Hz | ۲۸۸ GB/s ⚠️ | ۱۶ GB/s ✅ |
| ۱۰۰ Hz | ۲۸۸۰ GB/s ❌ | ۱۶۰ GB/s ⚠️ |

**نتیجه‌گیری‌ها:**
- سیم‌کشی محلی‌گرا واقعاً حیاتی است — بدون آن حتی ۱۰ Hz هم اتصال را خفه می‌کند
- **نرخ فایر باید زیر ~۱۰ Hz بماند.** خوشبختانه اقتصاد مانا خودش این را تحمیل می‌کند: نورون عادی با ۲۰ مانا و هزینه‌ی ~۱٫۵ در هر فایر متوسط، بدون درآمد فقط ~۱۳ فایر جا دارد.
- بالای ۱۰ Hz نیاز به فشرده‌سازی سیگنال (بسته‌بندی چند سیگنال هم‌مقصد در یک پیام) دارد.

**عامل محدودکننده در نهایت VRAM و پهنای باند است، نه توان محاسباتی.**

### ۱۲٫۷ چیدمان فایل‌ها — حداقل‌گرایی

**اصل: کمترین تعداد فایل ممکن.** تقسیم فقط وقتی انجام می‌شود که یک محدودیت فنی واقعی مجبورمان کند، نه به‌خاطر «معماری تمیز».

| مرحله | فایل‌ها | دلیل |
|---|---|---|
| ۰ تا ۳ (CPU) | `smile.cpp` — **یک فایل** | همه‌چیز: VM، نورون، اقتصاد، زمان‌بندی، کدک، دستگاه، داشبورد |
| ۳٫۵ به بعد (CUDA) | `smile.hpp` · `core.cu` · `host.cpp` — **سه فایل** | تقسیم بر اساس کامپایلر، نه بر اساس لایه‌بندی |

**داشبورد فایل جدا نمی‌گیرد:** HTML/JS به‌صورت رشته‌ی خام (`R"HTML(...)HTML"`) داخل همان فایل قرار می‌گیرد و با یک وب‌سرور کوچک سرو می‌شود. صفر فایل جانبی، صفر وابستگی.

**چرا در فاز CUDA سه فایل لازم می‌شود:**
- کد دستگاه و کد میزبان را دو کامپایلر متفاوت می‌سازند (`nvcc` و `g++`)
- تک‌فایل یعنی کامپایل مجدد کل پروژه در هر تغییر: برای ~۸٬۰۰۰ خط با CUDA حدود ۳۰ تا ۹۰ ثانیه، بدون امکان موازی‌سازی
- کتابخانه‌های سمت میزبان از داخل `nvcc` دردسرسازند

> تک‌فایل ماندن حتی در فاز CUDA **ممکن** است (همه‌چیز را به `nvcc` بدهیم)؛ فقط هزینه‌اش زمان کامپایل است. تصمیم نهایی هنگام رسیدن به آن مرحله گرفته می‌شود.

### ۱۲٫۸ خروجی نهایی: دو چیز، نه بیشتر

مدل‌های امروزی در ظاهر «یک فایل» هستند، ولی این تصویر گمراه‌کننده است:

| | مدل زبانی امروزی | smile |
|---|---|---|
| **موتور** | `llama.cpp` — بیش از ۱۰۰ فایل، ~۲۰۰ هزار خط | `smile.cpp` — **یک فایل** |
| **داده** | `model.gguf` — وزن‌های ثابت | `brain.dat` — حالت زنده‌ی مغز |

**GGUF کد نیست، داده است.** آن فایل تک به‌تنهایی هیچ کاری نمی‌کند؛ به یک موتور عظیم نیاز دارد. smile در واقع از آن مینیمال‌تر است.

```
smile.cpp ──کامپایل──▶ smile (~۵۰۰ KB)
                            │
                            ├── می‌خواند ──▶ brain.dat
                            └── می‌نویسد ──▶ brain.dat
```

**محتویات `brain.dat`:** حافظه‌ی تک‌تک نورون‌ها، مخزن بایت‌کد توابع، تمام یال‌ها و تأخیرهایشان، موجودی استخرها، ساعت مجازی، و بذر مولدهای تصادفی.

**اندازه:**

| نورون | `brain.dat` |
|---|---|
| ۵٬۰۰۰ (مرحله‌ی ۰) | ۰٫۹ مگابایت |
| ۱۰۰٬۰۰۰ | ۱۸٫۶ مگابایت |
| ۱۰ میلیون | ۱٫۹ گیگابایت |
| ۱ میلیارد | ۱۸۵ گیگابایت |

> برای مقایسه: Llama-3 8B کوانتیزه ≈ ۴٫۷ گیگابایت. یعنی smile با ۱۰ میلیون نورون از یک مدل ۸ میلیاردی **کوچک‌تر** است.

#### تفاوت بنیادی با GGUF

| GGUF | `brain.dat` |
|---|---|
| مرده و ثابت | **زنده و در حال تغییر** |
| هزار بار اجرا = هزار بار همان جواب | هر لحظه در حال دگرگونی |
| هر بار ذخیره = فایل یکسان | هر بار ذخیره = فایل متفاوت |
| قابل انتشار به‌عنوان محصول | **یک موجود**، نه یک محصول |

**پیامد عملی:** باید نسخه‌های زمان‌دار نگه داشت. اگر مغز پس از دو هفته آموزش خراب شود، تنها راه بازگشت چک‌پوینت قدیمی است — و آن یعنی دو هفته خاطره از دست رفته.

### ۱۲٫۹ تکرارپذیری

با همان بذر، همان اجرا — **حتی با تعداد کارت متفاوت**. برای این:
- ترتیب پردازش داخل هر پنجره نباید روی نتیجه اثر بگذارد (تجمیع جابه‌جایی‌پذیر)
- هر نورون مولد شبه‌تصادفی بذردار مستقل خودش را دارد (نه یک مولد سراسری)
- تمام حساب‌های مانا با عدد ثابت‌ممیز، نه اعشاری

غیرقابل مذاکره — بدون آن دیباگ کردن یک موجود ناهمگام نویزی ممکن نیست.

---

## ۱۳. مسیر ساخت

| مرحله | هدف | معیار موفقیت |
|---|---|---|
| ۰ | نمونه‌ی کوچک، ~۵٬۰۰۰ نورون | آیا اقتصاد پایدار می‌ماند یا همه می‌میرند؟ |
| ۱ | داشبورد + چک‌پوینت | می‌توانیم ببینیم و متوقف کنیم |
| ۲ | کدک فارسی + نمره‌دهی دستی | اولین کلمه‌ی معنادار |
| ۳ | مقیاس ۱۰۰٬۰۰۰ + چندنخی روی CPU | پایداری در مقیاس |
| ۳٫۵ | انتقال توده به GPU + سیم‌کشی محلی‌گرا | ۱۰⁷ نورون روی یک کارت |
| ۳٫۷ | چند کارت + ناحیه‌بندی | مقیاس‌گیری خطی تا ۱۰⁹ |
| ۴ | فازهای ۲ تا ۵ برنامه‌ی درسی | درک زبان |
| ۵ | جوانه‌زدن | رشد ساختاری |

**مرحله‌ی ۰ حیاتی است.** بدون آن ممکن است ماه‌ها C++ بنویسیم و بعد بفهمیم فرمول مانا از پایه غلط بوده.

---

## ۱۴. انتظار واقع‌بینانه

- **نمی‌رسیم به:** رقیب GPT به فارسی.
- **می‌رسیم به:** یک بستر زنده و قابل مطالعه — ارگانیسمی محاسباتی که خودش را زنده نگه می‌دارد، حافظه می‌سازد، رشد می‌کند، و می‌شود در آن پدیده‌های نوظهور را دید.

یادگیری **بسیار کندتر** از مدل‌های دیگر است، چون از راه درد می‌گذرد.

---

## ۱۵. سؤالات باز

### ۱۵٫۱ ضعف‌های برطرف‌شده

| # | ضعف | راه‌حل | بند |
|---|---|---|---|
| ۱ | تابع ذخیره‌ساز یک‌طرفه = نابودی حافظه در تعویض | دوطرفه + آزمون اجباری دوطرگی | ۷٫۲ |
| ۲ | گرسنگی لوب ورودی | درآمد پایه + نشت دوطرفه + نشت خودتنظیم | ۵٫۳ |
| ۳ | سکوت مطلق = تعادل پایدار | هزینه‌ی زنده‌ماندن ۲٪ + خواب زمستانی | ۵٫۴ |
| ۴ | موج مرگ زنجیره‌ای | اعتبار پایانی + قرنطینه + سقف نرخ مرگ ۰٫۱٪ | ۶ |
| ۵ | فروپاشی به پچ‌پچ کم‌رمق | هزینه‌ی راه‌اندازی ۰٫۵ + بازده فزاینده | ۲٫۴ |
| ۶ | مجموعه دستورات VM طراحی‌نشده | ۱۶ ثبات، ۴ بایت ثابت، سه سطح دسترسی | ۹٫۱ |
| ۷ | جهش می‌تواند برنامه را خراب کند | هر توالی بایت معتبر است (پیمانه + NOP) | ۹٫۱ |
| ۸ | فرمول هزینه‌ی غول | پایه × تفاوت × تکرار نمایی + بازگشت سرمایه | ۹٫۲ |
| ۹ | «لوب مرکزی تصمیم می‌گیرد» = ناظر مرکزی | مزایده‌ی غیرمتمرکز (پیشنهاد × اعتبار) | ۱۰ |
| ۱۰ | ۳۰ میلیون نورون حافظه‌ای روی CPU | انتقال به GPU با حلقه‌ی باز شده | ۱۲٫۲ |
| ۱۱ | غول‌های دو کارت مختلف | همه‌ی غول‌ها در RAM میزبان، نه VRAM | ۱۲٫۲ |
| ۱۲ | نگاشت لوب به ناحیه | تکه‌بندی عمودی: هر ناحیه از هر سه لوب | ۱۲٫۴ |
| ۱۳ | بودجه‌ی VRAM (۳۹۸ GB) | بایت‌کد مشترک با شناسه → ۱۴۶ GB | ۱۲٫۶ |
| ۱۴ | پهنای باند بین‌کارتی | محلی‌گرایی + سقف نرخ فایر ~۱۰ Hz | ۱۲٫۶ |
| ۱۵ | قفل سراسری استخر مانا | زیرحساب محلی + تسویه در مرز پنجره | ۱۲٫۵ |

### ۱۵٫۲ باقی‌مانده — پارامترهایی که فقط آزمایش تعیینشان می‌کند

اینها ضعف طراحی نیستند؛ اعدادی‌اند که مقدار اولیه دارند و در مرحله‌ی ۰ کالیبره می‌شوند:

| پارامتر | مقدار اولیه |
|---|---|
| نرخ نشت پاداش بین لوب‌ها | ۵۰٪ به عقب، ۱۰٪ به جلو |
| نرخ درآمد پایه | ۰٫۵ مانا بر نورون بر ثانیه |
| زمان بازگشت مانای در ترانزیت | ۰٫۵ ثانیه‌ی مجازی |
| هزینه‌ی زنده‌ماندن | ۲٪ سقف در ثانیه |
| سقف نرخ مرگ | ۰٫۱٪ جمعیت لوب در ثانیه |
| نسبت یال محلی به دوربرد | ۹۰ / ۱۰ |
| دمای نویز اولیه | بالا، سپس کاهش تدریجی |

### ۱۵٫۳ باقی‌مانده — پرسش‌های واقعاً باز

| # | پرسش | چرا هنوز باز است |
|---|---|---|
| ۱ | آیا بایت‌کد خطی برای نورون عادی به‌اندازه‌ی کافی رساست؟ | فقط با آزمایش اکو و کپی با تأخیر معلوم می‌شود |
| ۲ | آیا اقتصاد مانا به تعادل زنده می‌رسد یا به یکی از دو قطب (انفجار / انجماد) می‌افتد؟ | **پرسش بنیادی مرحله‌ی ۰** |
| ۳ | آیا غول‌ها واقعاً توابع بهتر کشف می‌کنند یا در بهینه‌ی محلی گیر می‌کنند؟ | نیازمند اجرای طولانی |
| ۴ | آیا نمره‌دهی سطح‌کلمه سیگنال کافی برای شکل‌گیری زبان دارد؟ | فاز ۱ برنامه‌ی درسی |

---

## ۱۶. مرحله‌ی صفر — نتایج اجرا

**وضعیت: ✅ اجرا شد.** `smile.cpp` — یک فایل، ۱٬۲۰۰ خط، بدون وابستگی.

```
g++ -O2 -std=c++17 -pthread smile.cpp -o smile
./smile --neurons 5000 --port 8080
```

### ۱۶٫۱ پاسخ به پرسش بنیادی

> **«آیا اقتصاد مانا به تعادل زنده می‌رسد یا به انفجار/انجماد می‌افتد؟»**

**پاسخ: به تعادل زنده می‌رسد — ولی نه با پارامترهای اولیه‌ی سند.**

| سنجه | نتیجه پس از ۹۰ ثانیه‌ی مجازی |
|---|---|
| جمعیت زنده | ۵۰۰۰ از ۵۰۰۰ (صفر مرگ) |
| نرخ فایر | ۲٫۶۶ هرتز بر نورون — **ثابت** |
| پرشدگی استخر | ۸۰٪ — **قفل‌شده** |
| نورون در حالت ایگنور | صفر |
| خطای تابع | صفر |
| توان عملیاتی | ~۸۱۰٬۰۰۰ رویداد در ثانیه (تک‌نخ) |

نرخ فایر ۲٫۶۶ هرتز **زیر سقف ۱۰ هرتزی** بند ۱۲٫۶ است — یعنی بودجه‌ی پهنای باند چند-GPU برقرار می‌ماند.

### ۱۶٫۲ سه باگ که فقط با اجرا پیدا شدند

**۱. ورشکستگی ساختاری رده‌های بالا.**
درآمد پایه سرانه بود (۰٫۵ مانا بر نورون) ولی هزینه‌ی زنده‌ماندن درصدی از سقف (بند ۵٫۴). نتیجه: نورون حافظه‌ای و غول از لحظه‌ی تولد بیشتر می‌سوزاندند از آنچه می‌گرفتند. **در اولین اجرا ۱۴۳ نورون از ۱۵۰ نورون حافظه‌ای مردند.**

> **اصلاح:** درآمد پایه با **مجموع ظرفیت** لوب وزن می‌خورد، نه با تعداد سر.

**۲. تورم مهارنشده.**
مانا از بین نمی‌رود (بند ۵٫۱) و درآمد پایه دائم اضافه می‌شود → استخر به ۱۶۵٪ رسید و همچنان بالا می‌رفت. مانای فراوان = بی‌ارزش = صفر فشار اقتصادی.

> **اصلاح:** سقف هومئوستاتیک. درآمد پایه و مانای بازگشته از ترانزیت فقط تا `هدف` وارد استخر می‌شوند؛ مازاد به خزانه برمی‌گردد.

**۳. گرسنگی لوب مرکزی.**
سند نگران لوب **ورودی** بود (بند ۵٫۳)، ولی در عمل **لوب مرکزی** خشکید: استخرش روی ۱٪ ماند در حالی که ورودی ۷۴٪ و پایانی ۸۳٪ بودند. علت: لوب مرکزی ۶۰٪ جمعیت و بیشترین ترافیک را دارد.

> **اصلاح:** (الف) نشت خودتنظیم بند ۵٫۳ واقعاً پیاده شد — استخر گرسنه ضریب بیشتری می‌مکد. (ب) جبران گرسنگی در درآمد پایه: زیر ۵۰٪ پرشدگی، درآمد تا ۴ برابر بالا می‌رود.

### ۱۶٫۳ آنچه تأیید شد

| ادعای سند | نتیجه |
|---|---|
| تکرارپذیری با بذر یکسان (۱۲٫۹) | ✅ بذر ۴۲ دو بار: `fires=117923 signals=1433010 events=7365435` — یکسان |
| پنجره‌ی موازی از حداقل تأخیر (۱۲٫۱) | ✅ ساختار رویدادمحور کار می‌کند؛ صف اولویت‌دار گلوگاه نشد |
| ماشین مجازی با سوخت (۹) | ✅ صفر کرش، صفر حلقه‌ی بی‌نهایت در ۷۳ میلیون رویداد |
| `SEL` به‌جای انشعاب (۱۲٫۲) | ✅ نورون‌های عادی با بایت‌کد خطی محض کار کردند |
| چک‌پوینت زنده (۱۲٫۸) | ✅ `brain.dat` — ۲٫۱ مگابایت برای ۵۰۰۰ نورون؛ بارگذاری و ادامه از همان نقطه |
| مقیاس‌پذیری | ✅ ۵۰٬۰۰۰ نورون همان تعادل ۶۷٪ را داد |

### ۱۶٫۴ ضعف‌های تازه‌کشف‌شده — برای مرحله‌ی بعد

| # | مشاهده | معنا |
|---|---|---|
| ۱ | **تنبیه اثر نمی‌کند** | تنبیه −۵۰۰ هیچ تغییری در رفتار نداد؛ جبران گرسنگی فوراً پرش می‌کند. لازم است تنبیه به‌جای استخر، مستقیماً روی **اعتبار** نورون‌های فعال اثر بگذارد |
| ۲ | **خروجی خالی است** | لوب پایانی بیت تولید می‌کند ولی به بایت معتبر نمی‌رسد. طبیعی است — بدون نردبان پاداش (بند ۱۱٫۳) انتظار دیگری نیست |
| ۳ | **مرگ هرگز رخ نداد** | مهارهای بند ۶ آن‌قدر قوی‌اند که فشار انتخاب صفر است. برای تکامل، باید فشار را کنترل‌شده بالا برد |
| ۴ | **غول‌ها هنوز بی‌کارند** | `PEEK/POKE` و شبکه‌ی دوم پیاده نشده‌اند |

### ۱۶٫۵ داشبورد

- نقشه‌ی استخر مانا به تفکیک لوب · شمارش زنده/سالم/ایگنور/اسپم/خواب/مرده
- نمودار زنده‌ی نرخ فایر، پرشدگی استخر، جمعیت
- کنترل سرعت زمان مجازی (۰٫۰۵× تا بیشینه) و دمای نویز
- دکمه‌ی پاداش و تنبیه دستی
- **دکمه‌ی خاموش کردن** — توقف مدل و نوشتن `brain.dat` روی دیسک
- داوری خودکار: تعادل زنده / فروپاشی / انجماد / انفجار

---

## ۱۷. اصلاحات پس از اولین بازخورد

### ۱۷٫۱ ✅ چرا خروجی خالی بود — سه باگ زنجیره‌ای

پرسش درست بود: «مغز باید مسیرهای مختلف را بیازماید تا بالاخره یکی خروجی باشد.» دقیقاً همین کار را نمی‌کرد. سه باگ روی هم:

**۱. ماسک هرگز به خطوط خروجی نمی‌رسید.**
ماسک با `base << k` ساخته می‌شد که فقط به بالا جابه‌جا می‌کرد. خطوط ۱۸ و ۱۹ — همان‌هایی که به خروجی واقعی وصل‌اند — عملاً هرگز روشن نمی‌شدند.
> **اصلاح: چرخش حقیقی.** `(base << k) | (base >> (20-k))` — حالا هر ۲۰ خط شانس برابر دارند. اندازه‌گیری: **۲۹٫۸٪ فایرها خط ۱۸ یا ۱۹ را شامل می‌شوند.**

**۲. خروجی از بیت‌های اشتباه خوانده می‌شد.**
کد بیت‌های ۰ و ۱ الگو را برمی‌داشت، ولی خطوط خروجی ۱۸ و ۱۹ بودند.
> **اصلاح:** خروجی از همان دو خط پایانی خوانده می‌شود که واقعاً فایر شده‌اند.

**۳. الگوی بیت در خطوط بالا همیشه صفر بود.**
منبع الگو نویز ۸ بیتی بود؛ بیت‌های ۱۸ و ۱۹ همیشه صفر می‌ماندند. نتیجه: **۲٬۴۴۶ بیت خروجی، همه صفر.**
> **اصلاح:** نویز با سه جابه‌جایی در تمام ۲۴ بیت پخش می‌شود.

**نتیجه‌ی پس از اصلاح:**

| سنجه | پیش از اصلاح | پس از اصلاح |
|---|---|---|
| بیت‌های خروجی | ۲٬۴۴۶ (همه صفر) | ۲٬۲۴۲ (۵۴٪ یک) |
| بایت معتبر | **۰ از ۲۸۰** | **۱۳۵ از ۲۸۰** |

خروجی زنده‌ی فعلی نمونه: `ERO^bYfIUNfdvlUUPUzjYrFu`kVm~VJUR_NveQG6aRWUoUUD_ZiWijPY}D`

هنوز آشغال است — و باید باشد. بدون نردبان پاداش (بند ۱۱٫۳) دلیلی ندارد معنادار باشد. ولی **کانال باز است**: مغز مسیرها را می‌آزماید، بیت تولید می‌کند، بایت معتبر می‌سازد. حالا چیزی هست که بشود به آن نمره داد.

### ۱۷٫۲ ✅ هزینه‌ی اعتبار — نگرانی درست بود

نگرانی: «اعتبار دادن به هر نورون پروژه را سنگین می‌کند.»

حساب دقیق: با `i64` هشت بایت، در ۱۰⁹ نورون **۸ گیگابایت** فقط برای این یک فیلد.

> **اصلاح: `u16` به‌جای `i64`.** ۲ بایت، اشباع در ۶۵۵۳۵، محو با شیفت به‌جای تقسیم.
> صرفه‌جویی در مقیاس ۱۰⁹: **۶ گیگابایت** (۸ GB → ۲ GB).

از نظر عملیاتی هم سبک است: یک جمع و یک شیفت در هر فایر، بدون تخصیص حافظه و بدون پیمایش. اعتبار در همان خط کش نورون می‌نشیند که مانا.

### ۱۷٫۳ پورت پیش‌فرض

از ۸۰۸۰ به **۸۴۲۰** تغییر کرد (۸۰۸۰ معمولاً اشغال است).

### ۱۷٫۴ اجرای یک‌خطی روی ویندوز

`run.ps1` اضافه شد: کامپایلر را بررسی/نصب می‌کند، کد را می‌گیرد، کامپایل می‌کند، اجرا می‌کند و داشبورد را باز می‌کند. کد شبکه‌ی `smile.cpp` حالا هم POSIX و هم Winsock را پشتیبانی می‌کند.

### ۱۷٫۵ غول‌ها — به تعویق افتاد

مورد تأیید: غول‌ها به این زودی به کار نمی‌افتند. `PEEK/POKE`، شبکه‌ی دوم، و مزایده‌ی طلا به فاز بعد موکول شد. در حال حاضر غول‌ها فقط مثل نورون‌های بزرگ رفتار می‌کنند.

### ۱۷٫۶ اصلاح اسکریپت ویندوز

نسخه‌ی اول بی‌صدا گیر می‌کرد. سه علت:

| مشکل | چرا گیر می‌کرد | اصلاح |
|---|---|---|
| نصب MSYS2 با winget | ~۱۰۰ MB دانلود + `pacman` در پنجره‌ی پنهان — بدون هیچ بازخوردی | w64devkit قابل حمل، با تأیید کاربر و نمایش پیشرفت |
| `git clone` بدون بررسی | اگر git نبود، خطای مبهم | فقط `smile.cpp` مستقیم دانلود می‌شود (۶۰ KB) |
| نبود بازخورد مرحله‌ای | کاربر نمی‌دانست کجا گیر کرده | پنج مرحله‌ی شماره‌دار با پیام فارسی |

**تغییرات دیگر:**
- بدون نیاز به دسترسی ادمین — کامپایلر داخل پوشه‌ی خود پروژه
- بررسی مسیرهای رایج کامپایلر پیش از هر دانلودی
- انتظار فعال تا پاسخ سرور (تا ۱۵ ثانیه) به‌جای `Sleep` کور
- خروجی کامپایلر در صورت خطا نمایش داده می‌شود
- `-static` تا exe بدون DLL جانبی اجرا شود
- تشخیص پورت اشغال با پیام فارسی، به‌جای شکست بی‌صدا

### ۱۷٫۷ پنجره‌ی کنسول — سه اصلاح

**۱. برنامه بسته نمی‌شد — فقط ساکت بود.**
لاگ کاربر با `داشبورد: http://0.0.0.0:8420` تمام می‌شد، یعنی سرور بالا آمده بود. ولی پنجره پس از آن هیچ خروجی‌ای نمی‌داد و به‌نظر می‌رسید برنامه بسته شده.
> **اصلاح:** گزارش زنده هر ۲ ثانیه در کنسول — زمان، جمعیت، نرخ فایر، استخر، رویداد بر ثانیه. حالا زنده‌بودن مغز دیده می‌شود.

**۲. متن فارسی خراب نمایش داده می‌شد** (`ΓòöΓòÉΓòÉ...`).
کنسول ویندوز پیش‌فرض UTF-8 نیست.
> **اصلاح:** `SetConsoleOutputCP(CP_UTF8)` در شروع برنامه + `chcp 65001` در اسکریپت.
> ضمناً سرستون‌های جدول به لاتین کوتاه تغییر کرد، چون عرض کاراکتر فارسی در کنسول ستون‌ها را به‌هم می‌ریزد.

**۳. مرورگر از داخل خود برنامه باز می‌شود.**
پیش‌تر وابسته به اسکریپت پاورشل بود؛ اجرای دستی مرورگر باز نمی‌کرد.
> **اصلاح:** پس از بالا آمدن سرور، `ShellExecuteA` (ویندوز) / `xdg-open` (لینوکس) / `open` (مک).
> با `--no-browser` می‌توان غیرفعالش کرد.

**همچنین:** `0.0.0.0` در پیام به `localhost` تغییر کرد (قابل کلیک)، و هشدار «این پنجره را باز نگه دارید» اضافه شد.

---

## ۱۸. رابط گفتگو و نمره‌دهی — فاز ۱ برنامه‌ی درسی

**وضعیت: ✅ پیاده شد.** حلقه‌ی کامل انسان↔مغز حالا بسته است.

### ۱۸٫۱ آنچه کم بود

تا پیش از این، دستگاه **فقط خروجی** می‌گرفت. سه قطعه‌ی گمشده ساخته شد:

| قطعه | کار |
|---|---|
| **حواس** | متن فارسی → UTF-8 → بیت → دو خط پایانی نیمه‌ی الف لوب ورودی |
| **آینه** | هر بایتی که مدل گفت، با تأخیر به نیمه‌ی ب لوب ورودی برمی‌گردد (بند ۴) |
| **داور** | نمره‌ی کلمه → فشرده‌سازی لگاریتمی → استخر لوب پایانی → نشت به عقب |

### ۱۸٫۲ قطعه‌بندی کلمه

طبق بند ۱۱٫۱: **فاصله جداکننده است و به کلمه‌ی قبلش می‌چسبد.**

```
بیت‌های خروجی → بایت → کاراکتر چاپی؟ به کلمه اضافه کن
                      → فاصله؟ کلمه را ببند و برای نمره‌دهی بفرست
                      → بیش از ۲۴ کاراکتر؟ اجباراً ببند (تا نمره‌دهی ممکن بماند)
```

### ۱۸٫۳ رابط

- **جریان زنده:** کلمات مدل و پیام‌های انسان به ترتیب زمانی، در یک نوار
- **کلیک روی کلمه** → پنجره‌ی نمره: −۱۰۰۰ تا +۱۰ به‌صورت دکمه، به‌علاوه‌ی ورودی دلخواه (بازه‌ی نامحدود)
- **کلیک راست** → نمره‌ی سریع +۱۰
- **رنگ:** سبز برای نمره‌ی مثبت، قرمز برای منفی
- **جعبه‌ی پیام** برای گفتن به مدل
- آمار زنده: کل کلمات، تعداد نمره‌داده‌شده، میانگین نمره

### ۱۸٫۴ ✅ ضعف #۱ بند ۱۶٫۴ حل شد — تنبیه حالا اثر می‌کند

پیش‌تر تنبیه سراسری (`/reward?v=-500`) هیچ اثری نداشت، چون جبران گرسنگی فوراً استخر را پر می‌کرد.

با نمره‌دهی کلمه‌محور، آزمایش واقعی:

| کنش | استخر لوب ورودی |
|---|---|
| وضعیت پایه | ۶۳٪ |
| نمره‌ی +۱۰ به یک کلمه | ۱۰۰٪ (لوب پایانی و مرکزی هم پر شد) |
| تنبیه −۱۰۰ روی ۶ کلمه | **۹٪** |

تفاوت در این است که نمره حالا **مکرر و متصل به رویداد مشخص** است، نه یک ضربه‌ی منفرد.

### ۱۸٫۵ نکته‌ی مهم درباره‌ی انتظار

مدل در این مرحله **آشغال تولید می‌کند** و باید بکند:

```
de$Y%EiTeUZf&VDb%ZiQibfU   EfRAQ   &d%^ebi   RAR%TUHiXeVWb%Zhe
```

بدون نردبان پاداش، دلیلی ندارد چیز دیگری بگوید. آنچه اهمیت دارد این است که **حلقه بسته است**: مدل می‌گوید، ما نمره می‌دهیم، نمره به مانا تبدیل می‌شود، مانا تعیین می‌کند کدام نورون زنده بماند.

پهنای باند معلم همان محدودیتی است که در بند ۱۱٫۳ گفته شد: ~۶۰ نمره در دقیقه. برای همین فاز ۱ باید کوتاه باشد و هرچه زودتر به فاز ۲ (دیکشنری خودکار) رسید.

---

## ۱۹. سه ایراد بنیادی که در اجرای واقعی دیده شد

### ۱۹٫۱ ❌→✅ «مدل فقط انگلیسی حرف می‌زند»

**ریشه:** رمزگشا این فیلتر را داشت:

```cpp
if (byte >= 32 && byte < 127) ...   // فقط ASCII
```

فارسی در UTF-8 **دوبایتی** است (`0xD8–0xDB` + بایت ادامه‌ی `0x80–0xBF`). هر دو بایت بزرگ‌تر از ۱۲۷ بودند، پس **هر حرف فارسی عمداً دور ریخته می‌شد.** مدل هرگز شانس فارسی گفتن نداشت — کانال فارسی از پایه بسته بود.

**اصلاح: رمزگشای واقعی UTF-8 با ماشین حالت.**

```
بایت < 0x80          → ASCII، مستقیم
(byte & 0xE0)==0xC0  → شروع دوبایتی  (فارسی، عربی)
(byte & 0xF0)==0xE0  → شروع سه‌بایتی
(byte & 0xF8)==0xF0  → شروع چهاربایتی
(byte & 0xC0)==0x80  → بایت ادامه
دنباله‌ی ناقص یا نامعتبر → دور ریخته و در chars_bad شمرده می‌شود
```

**نتیجه:** حروف فارسی و کاراکترهای چندبایتی اکنون در خروجی ظاهر می‌شوند.

### ۱۹٫۲ ❌→✅ «فشار مانا تقریباً نمی‌آید»

مشاهده درست بود. حساب دقیق نشان داد اقتصاد از پایه شل بود:

| | پیش | پس |
|---|---|---|
| درآمد پایه (بر نورون بر ثانیه) | ۰٫۵۰ | ۰٫۳۴ |
| هزینه‌ی زنده‌ماندن | ۰٫۴۰ | ۰٫۴۰ |
| **مازاد بدون هیچ کاری** | **+۰٫۱۰** ❌ | **−۰٫۰۶** ✅ |
| هزینه‌ی راه‌اندازی فایر | ۰٫۵ | **۱٫۲** |
| هزینه‌ی هر خط | ۰٫۲ | **۰٫۳** |
| هدف استخر | ۱٫۵× مخازن | **۰٫۶×** |
| جبران گرسنگی | تا **۴×** | تا ۲× |
| سهم پایه از استخر | ۴۰٪ | **۱۵٪** (بقیه وابسته به اعتبار) |

**مهم‌ترین تغییر:** درآمد پایه حالا **کمتر از** هزینه‌ی زنده‌ماندن است. یعنی سکوت مطلق دیگر تعادل پایدار نیست — نورونی که هیچ کاری نکند آرام می‌میرد. این همان چیزی است که بند ۵٫۴ می‌خواست ولی عملاً محقق نشده بود.

**نتیجه‌ی اندازه‌گیری‌شده:**

| | پیش | پس |
|---|---|---|
| مرگ در ۶۰ ثانیه | **۰** | **۱۳۵** |
| استخر در تعادل | ۹۷٪ (سرریز) | ۵۰–۵۵٪ |

حالا انتخاب طبیعی واقعاً کار می‌کند: ۱۳۵ نورون که نتوانستند خرج خود را دربیاورند حذف شدند، سپس جمعیت پایدار ماند.

### ۱۹٫۳ ❌→✅ «بیشتر شبیه اسپم است تا تصمیم به خروج»

سه علت داشت:

**۱. فایر عملاً رایگان بود** — با مازاد مثبت درآمد، نورون می‌توانست بی‌وقفه شلیک کند. حل شد در ۱۹٫۲.

**۲. دوره‌ی تعلیق نداشت.** نورون می‌توانست بلافاصله پس از فایر دوباره شلیک کند.
> **اصلاح:** `REFRACTORY = 40ms` — پس از هر فایر، نورون تا ۴۰ میلی‌ثانیه‌ی مجازی نمی‌تواند دوباره شلیک کند. این در مغز واقعی هم هست.

**۳. کلمات بی‌نهایت بلند می‌شدند.** احتمال تصادفی تولید بایت فاصله ۱/۲۵۶ است، ولی در زبان واقعی فاصله ~۱۷٪ کاراکترهاست. نتیجه: کلمات ۴۰ کاراکتری که نمره دادن به آن‌ها بی‌معنی بود.
> **اصلاح:** سقف ۱۶ بایت برای طول کلمه. میانگین طول از ۲۸ به ۱۶ رسید و تعداد کلمات قابل نمره‌دهی تقریباً دو برابر شد.

---

## ۲۰. الفبای فارسی — چرا بایت خام هرگز جواب نمی‌داد

### ۲۰٫۱ ریشه‌ی واقعی «فقط انگلیسی حرف می‌زند»

در بند ۱۹٫۱ رمزگشای UTF-8 درست شد، ولی مدل **باز هم** انگلیسی حرف می‌زد. علت عمیق‌تر بود:

| نوع خروجی | احتمال تصادفی از بایت خام |
|---|---|
| ASCII چاپی | **۳۷٪** |
| دنباله‌ی معتبر فارسی | **۳٪** |

انگلیسی **۱۲ برابر** محتمل‌تر است. بدتر: هر حرف فارسی به **دو بایت مشخص پشت سر هم** نیاز دارد (`0xD8–0xDB` سپس `0x80–0xBF`). احتمال اینکه شبکه‌ی تصادفی این الگو را پیدا کند عملاً صفر است.

> **رمزگشا مشکل نبود؛ الفبا مشکل بود.**

سند از ابتدا می‌گفت «تابعی که صفر و یک را به زبان فارسی تبدیل کند» — هیچ‌جا نگفته بود آن تابع باید UTF-8 خام باشد. انتخاب بایت‌محور اشتباه پیاده‌سازی بود.

### ۲۰٫۲ راه‌حل: الفبای ۶ بیتی

**۶ بیت = ۶۴ نماد، همه فارسیِ معتبر:**

```
فاصله · ا ب پ ت ث ج چ ح خ د ذ ر ز ژ س ش ص ض ط ظ ع غ ف ق ک گ ل م ن و ه ی
آ أ ؤ ئ ة ء · ، . ؟ ! : ؛ - « » · ۰۱۲۳۴۵۶۷۸۹
```

هر خروجی مدل، از همان لحظه‌ی اول، **فارسیِ خوانا** است. فضای جست‌وجو از ۲۵۶ به ۶۴ کوچک شد و کل آن مفید است.

ورودی هم از همین نگاشت معکوس عبور می‌کند: متن فارسی → شاخص ۶ بیتی → بیت.

**نتیجه‌ی اندازه‌گیری‌شده:**

| | پیش | پس |
|---|---|---|
| کلمات دارای حرف فارسی | ۲ از ۲۹ | **۴۰ از ۴۰ (۱۰۰٪)** |

نمونه‌ی خروجی واقعی: `شت؟آع.ةثءض` · `أگخاک!اأبی` · `کگ` · `ضغءکدتقغآة`

هنوز بی‌معناست — و باید باشد. ولی حالا **در فضای درستی** بی‌معناست: هر نماد یک حرف فارسی است، پس نمره‌دهی معنا پیدا می‌کند.

### ۲۰٫۳ اسپم — سه اصلاح دیگر

**۱. نویز نباید به‌تنهایی باعث فایر شود.** در برنامه‌ی بذر، سهم نویز در برانگیختگی تا ۴۲ واحد می‌رسید در حالی که آستانه ۷۰ بود — یعنی نویز به‌تنهایی نزدیک بود نورون را بترکاند.
> وزن نویز به یک‌سوم کاهش یافت (`/6` → `/18`)، وزن ورودی و تازگی بالا رفت (۲۴→۴۰ و ۴۰→۵۵).

**۲. تعلیق بلندتر برای لوب پایانی.** هر فایر لوب خروجی مستقیماً یک نماد است، پس حرف زدن باید تصمیم باشد نه بازتاب.
> `REFRACTORY × 6` برای لوب پایانی (۲۴۰ میلی‌ثانیه در برابر ۴۰).

**نتیجه:** نرخ فایر از ۱٫۶ به ۰٫۹–۱٫۳ رسید.

### ۲۰٫۴ ✅ نشت خزانه — یک باگ جدی

هنگام بررسی خفگی لوب‌های عقب، معلوم شد مانای مازاد (وقتی استخری پر است) به `treasury` می‌رفت و **هرگز خرج نمی‌شد**. یک نشت واقعی که کل اقتصاد را می‌خشکاند.

> **اصلاح:** مازاد به **گرسنه‌ترین لوب** می‌رود، نه به خزانه.
> **نتیجه:** نورون‌های در حالت ایگنور از ۹ به **صفر** رسید.

### ۲۰٫۵ ⚠️ مسئله‌ی باقی‌مانده: تمرکز پاداش در لوب پایانی

استخر لوب پایانی روی ۱۰۰٪ می‌ماند و لوب ورودی/مرکزی حوالی ۱–۲٪. علت ساختاری است: پاداش فقط وارد لوب پایانی می‌شود و نشت به عقب کافی نیست.

این یک **تنش طراحی** است، نه صرفاً باگ:
- اگر لوب‌های عقب مستقل تغذیه شوند، ارتباطشان با کیفیت خروجی قطع می‌شود
- اگر کاملاً وابسته بمانند، از گرسنگی فلج می‌شوند

فعلاً با درآمد پایه و سرریز زنده می‌مانند (صفر ایگنور، صفر مرگ پس از تثبیت)، ولی توزیع مانا بین لوب‌ها هنوز نیازمند بازنگری است.

---

## ۲۱. چرا پنج تنبیه هیچ اثری نداشت

### ۲۱٫۱ عدد دقیق

مشاهده: «پنج تا −۱۰ دادم ولی لوب پایانی همین‌طور رفت بالا تا پر شد.»

حساب نشان داد تنبیه اصلاً «کار نکرد» نبود — **گرد شد به صفر**:

| | مقدار |
|---|---|
| نمره‌ی −۱۰ پس از فشرده‌سازی | −۷٫۲ مانا |
| پنج تا روی هم | −۳۶ مانا |
| هدف استخر لوب پایانی | ۱۱٬۸۸۰ مانا |
| **اثر نسبی** | **۰٫۳٪** |
| درآمد پایه‌ی همان لوب | ۳۳۷ مانا در ثانیه |
| **زمان جبران** | **۰٫۱ ثانیه** |

### ۲۱٫۲ سه ریشه

**۱. فشرده‌ساز لگاریتمی نمره‌های کوچک را له می‌کرد.**
ضریب ثابت ۳ بود: `log(1+10) × 3 = ۷٫۲ مانا`. لگاریتم برای مهار نمره‌های نجومی (−۱۰۰۰) لازم است، ولی نباید نمره‌ی معمولی را بی‌اثر کند.
> **اصلاح:** اثر نمره با **اندازه‌ی لوب** مقیاس می‌گیرد، نه عدد ثابت. یک نمره‌ی ±۱۰ حدود ۴۰٪ استخر لوب پایانی را جابه‌جا می‌کند.
> `mag = log(1+|s|)/log(11)` → نمره‌ی ۱۰ ضریب ۱٫۰ می‌گیرد، ۱۰۰۰ حدود ۲٫۹ — پس بازه‌ی نامحدود حفظ می‌شود ولی فشرده.

**۲. درآمد پایه تنبیه را فوراً می‌شست.**
هر تنبیهی در کسری از ثانیه جبران می‌شد.
> **اصلاح: دوره‌ی بدهی.** پس از تنبیه، لوب تا ۱۲ ثانیه‌ی مجازی درآمد پایه‌اش **یک‌چهلم** می‌شود. سرریز و مازاد ترانزیت هم در این مدت به آن لوب نمی‌رسند.
> قرینه‌اش هم اضافه شد: پس از پاداش، لوب‌های عقب تا ۸ ثانیه درآمد **سه برابر** می‌گیرند.

**۳. کف حیاتی زیادی بالا بود.** پیش‌تر ۲۰٪ بود؛ به ۵٪ کاهش یافت تا درد واقعی حس شود ولی شبکه نمیرد.

### ۲۱٫۳ نتیجه‌ی اندازه‌گیری‌شده

پنج تنبیه −۱۰، دقیقاً همان کاری که کاربر کرد:

| زمان | استخر لوب پایانی | نرخ فایر |
|---|---|---|
| پایه | ۱۰۰٪ | ۱٫۱۵ |
| ۲ ثانیه بعد | **۴۸٪** | ۱٫۱۰ |
| ۸ ثانیه بعد | ۶۱٪ | **۰٫۳۴** |
| ۱۶ ثانیه بعد | ۵۷٪ | ۰٫۸۷ |
| ۲۶ ثانیه بعد | ۹۸٪ | ۰٫۹۱ |

استخر نصف شد، ۲۶ ثانیه پایین ماند، و **نرخ فایر به یک‌سوم رسید** — یعنی مدل واقعاً ساکت شد، نه فقط عددی در داشبورد تغییر کرد.

> بازگشت تدریجی عمدی است: تنبیه باید درد داشته باشد، نه اینکه شبکه را برای همیشه فلج کند.

---

## ۲۲. چرا استخر بدون هیچ کاری پر می‌شد

مشاهده: «لوب پایانی در چند ده ثانیه بدون اینکه کاری کنم به ۱۰۰٪ رسید.»

حساب نشان داد هر سه لوب دقیقاً در **۳۵ ثانیه** پر می‌شوند — همان «چند ده ثانیه».

### ۲۲٫۱ سه باگ ساختاری

**۱. درآمد پایه می‌توانست استخر را تا ۱۰۰٪ پر کند.**
«پایه» باید نان شب باشد، نه ثروت. اگر درآمد خودکار به سقف برسد، پاداش بی‌معنا می‌شود — شبکه بدون هیچ کاری ثروتمند است.

> **اصلاح: سقف معیشت.** درآمد پایه فقط تا **۲۵٪** هدف کار می‌کند (لوب ورودی ۴۵٪، چون ته زنجیره‌ی نشت است). بالاتر از آن، **تنها پاداش** می‌تواند استخر را پر کند.

**۲. درآمد سرانه بود، ولی خرج لوب‌ها یکسان نیست.**
لوب پایانی تعلیق ۶ برابر دارد → کم خرج می‌کند → انباشت می‌کند. لوب مرکزی پرترافیک‌ترین است → همیشه گرسنه.

> **اصلاح:** درآمد به **تقاضای واقعی** گره خورد (`burn_recent`: مانای سوخته‌ی اخیر با محو تدریجی). لوبی که خرج نمی‌کند، انباشت نمی‌کند.

**۳. لوب پایانی یک مخزن بن‌بست بود.**
شرط سرریز `pool > target` بود، ولی استخر دقیقاً روی `target` بسته می‌شد — پس **هرگز سرریز نمی‌کرد**.

> **اصلاح:** سرریز از **۴۰٪** هدف شروع می‌شود، نه ۱۰۰٪. مانا در سیستم می‌چرخد به‌جای اینکه در انتها تلنبار شود.

### ۲۲٫۲ باگ چهارم: تنبیه در ۵ ثانیه شسته می‌شد

پس از اصلاحات بالا، تنبیه استخر را به ۵٪ می‌برد ولی **ظرف ۵ ثانیه به ۱۰۰٪ برمی‌گشت** — بالاتر از نقطه‌ی شروع.

علت: بازگشت مانای در ترانزیت (بند ۵٫۲) دوره‌ی بدهی را نادیده می‌گرفت و استخر تنبیه‌شده را دوباره پر می‌کرد.

> **اصلاح:** مانای بازگشتیِ لوبِ در دوره‌ی بدهی **واقعاً می‌سوزد** — نه به خودش می‌رسد، نه به لوب دیگری منتقل می‌شود.
> نکته‌ی ظریف: در تلاش اول آن را به گرسنه‌ترین لوب فرستادیم، ولی نتیجه این شد که لوب ورودی به ۱۰۰٪ پرید. تنبیه نباید ثروت را **جابه‌جا** کند، باید **نابود** کند.

### ۲۲٫۳ نتیجه

**بدون دخالت:**

| زمان مجازی | استخر |
|---|---|
| ۲ ثانیه | ۳٪ |
| ۲۰ ثانیه | ۲۲٪ |
| ۷۱ ثانیه | **۲۳٪ — ثابت** |

دیگر هرگز به ۱۰۰٪ نمی‌رسد. سقف طبیعی حدود ۲۳٪ است و بالاتر از آن فقط با پاداش ممکن می‌شود.

**با پنج تنبیه −۱۰:**

| زمان | لوب پایانی | لوب مرکزی |
|---|---|---|
| پایه | ۴۱٪ | ۲۶٪ |
| +۵ ثانیه | **۰٪** | **۰٪** |
| +۱۰ ثانیه | ۰٪ | ۰٫۵٪ |
| +۱۵ ثانیه | ۱٪ | ۱٪ |
| +۲۰ ثانیه | ۳۰٪ | ۱۱٪ |
| +۳۰ ثانیه | ۴۱٪ | ۲۵٪ |

تنبیه کل اقتصاد را می‌خواباند و **۱۵ ثانیه** پایین نگه می‌دارد، سپس تدریجی بازمی‌گردد.

---

## ۲۳. کالیبراسیون تنبیه — یک نمره نباید قتل عام باشد

مشاهده: «یک دونه منفی ۱۰ می‌دم، ۱۰۰ تا نورون می‌میرن.»

درست بود. در بند ۲۱ تنبیه را تقویت کردم چون بی‌اثر بود، ولی **بیش از حد** تقویت شد:

| نمره | اثر پیشین | بدهی پیشین |
|---|---|---|
| −۱۰ | **۴۰٪ استخر** | **۱۲ ثانیه، درآمد ۱/۴۰** |

این دو با هم یعنی استخر خالی می‌ماند و نورون‌ها دسته‌جمعی از گرسنگی می‌میرند.

### ۲۳٫۱ اصلاحات

| پارامتر | پیش | پس |
|---|---|---|
| اثر نمره بر استخر | ۴۰٪ | **۲۰٪** |
| مدت بدهی | ۱۲ ثانیه | **۳ ثانیه** |
| شدت بدهی | درآمد ÷۴۰ | **÷۳** |
| کف حیاتی | ۵٪ | **۱۲٪** |
| مانای ترانزیت در بدهی | کامل می‌سوخت | **نصفش برمی‌گردد** |

### ۲۳٫۲ سپر مرگ — تضمین سخت

مهم‌ترین اصلاح: پارامترها به‌تنهایی کافی نبودند، چون استخر خالی مساوی است با نورون گرسنه.

> **در دوره‌ی تنبیه (و تا ۵ ثانیه پس از آن)، هیچ نورونی از گرسنگی نمی‌میرد.**

تنبیه معنایش «ساکت شو» است، نه «بمیر». مرگ باید از ناکارآمدی مزمن بیاید، نه از یک نمره‌ی منفی.

### ۲۳٫۳ نتیجه

| نمره | نرخ فایر (پایه ۲٫۸۵) | استخر | مرگ |
|---|---|---|---|
| −۱ | ۲٫۸۵ | ۴۱٪ | **۰** |
| −۱۰ | ۲٫۱۶ | ۳۴٪ | **۰** |
| −۱۰۰ | ۱٫۳۰ | ۱۲٪ | **۰** |
| −۱۰۰۰ | ۱٫۱۴ | ۱۳٪ | **۰** |

یک گرادیان واقعی: −۱ تقریباً هیچ، −۱۰ محسوس، −۱۰۰ شدید — و **هیچ‌کدام کسی را نمی‌کشند**.

بازیابی حدود ۱۰ ثانیه طول می‌کشد. مرگ همچنان وجود دارد (۷۲ نورون در ۳۰ ثانیه‌ی اول، از ناکارآمدی) ولی دیگر نتیجه‌ی مستقیم نمره‌دهی نیست.

---

## ۲۴. ریشه‌ی واقعی اسپم: تعداد بلندگو، نه نرخ فایر

سه بار نرخ فایر را مهار کردیم (تعلیق، وزن نویز، هزینه) و باز هم خروجی سیل بود. اندازه‌گیری نشان داد مشکل جای دیگری بود:

```
۹۹۰ نورون لوب پایانی × ۱٫۴۷ هرتز = ۱٬۴۵۵ نماد در ثانیه ≈ ۱۴۵ کلمه در ثانیه
انسان حداکثر: ~۱ کلمه در ثانیه
```

یعنی **۹۹٪ خروجی هرگز نمره نمی‌گرفت.** حتی با نرخ فایر کاملاً معقول، وقتی ۹۹۰ نورون همزمان بلندگو باشند نتیجه سیل است.

> مشکل «هر نورون چقدر حرف می‌زند» نبود؛ **«چند نفر حق حرف زدن دارند»** بود.

### ۲۴٫۱ قیف خروجی

در مغز واقعی هم مسیر حرکتی باریک است: میلیون‌ها نورون قشری به چند صد نورون حرکتی ختم می‌شوند. اینجا هم باید قیف باشد، نه دیوار.

> **فقط ~۳٪ لوب پایانی «نورون دهان» است** — یعنی به خروجی واقعی وصل می‌شود. با کف ۸ و سقف ۴۰.

بقیه‌ی لوب پایانی همچنان فعال است و در پردازش شرکت می‌کند، ولی صدایش مستقیماً بیرون نمی‌رود.

تعلیق سنگین (۱۲ برابر) هم از کل لوب به همین نورون‌های دهان منتقل شد — جایی که واقعاً لازم است.

### ۲۴٫۲ نتیجه

| | پیش | پس |
|---|---|---|
| نورون‌های متصل به خروجی | ۹۹۰ | **۴۰** |
| کلمه در ثانیه | ~۱۴۵ | **۰٫۲** |
| قابل نمره‌دهی توسط انسان | ۱٪ | **۱۰۰٪** |

حالا مدل حدود **یک کلمه هر ۵ ثانیه** می‌گوید — سرعتی که واقعاً می‌شود خواند و نمره داد.

---

## ۲۵. مقیاس ۳۲٬۰۰۰ نورون

نسبت‌های بند ۲٫۱ دقیقاً حفظ شدند:

| رده | تعداد | نسبت |
|---|---|---|
| عادی | ۳۰٬۹۹۲ | ۹۶٫۸۵٪ |
| حافظه‌ای | ۹۶۰ | ۳٫۰۰٪ |
| غول‌پیکر | ۴۸ | ۰٫۱۵٪ (۱ به ازای هر ۲۰ حافظه‌ای) |

**یال‌ها:** ۶۶۰٬۰۸۸ · **حافظه:** ~۶ مگابایت · **`brain.dat`:** ~۱۲ مگابایت

### پایداری در مقیاس

| زمان مجازی | زنده | مرده | استخر | نرخ فایر |
|---|---|---|---|---|
| ۱۲ ثانیه | ۳۱٬۵۶۵ | ۴۳۵ | ۴٪ | ۴٫۵ |
| ۳۰ ثانیه | ۳۱٬۵۵۷ | ۴۴۳ | ۲۵٪ | ۴٫۴ |
| ۶۰ ثانیه | ۳۱٬۵۵۷ | ۴۴۳ | **۲۸٪ — ثابت** | ۴٫۲ |

**۴۴۳ مرگ (۱٫۴٪)** در موج اول — همان نورون‌هایی که نتوانستند خرج خود را دربیاورند — سپس جمعیت کاملاً پایدار.

**توان عملیاتی: ~۲ میلیون رویداد در ثانیه** (تک‌نخ، شش برابر مقیاس ۵ هزار).

استخرها متعادل ماندند: ورودی ۱۸٪ · مرکزی ۲۲٪ · پایانی ۲۴٪.

پیش‌فرض `--neurons` به ۳۲۰۰۰ تغییر کرد.

---

## ۲۶. پهنای باند حسی/حرکتی ثابت است

در بند ۲۴ قیف خروجی را «۳٪ لوب پایانی» تعریف کردیم. این اشتباه بود: با بزرگ شدن مغز، دهان هم بزرگ می‌شد.

> **تعداد کانال‌های ورودی و خروجی یک ثابت فیزیکی است، نه نسبتی از اندازه‌ی مغز.**

مثل عصب بینایی: با بزرگ‌تر شدن قشر بینایی، تعداد رشته‌های عصب بینایی عوض نمی‌شود. مغز بزرگ‌تر یعنی **پردازش عمیق‌تر**، نه دهان بزرگ‌تر یا گوش بیشتر.

### ۲۶٫۱ ثابت‌ها

```cpp
MOUTH_COUNT  = 24    // نورون‌های متصل به خروجی واقعی
EAR_COUNT    = 48    // گیرنده‌های ورودی انسان (نیمه‌ی الف)
MIRROR_COUNT = 48    // گیرنده‌های آینه (نیمه‌ی ب)
```

اثبات استقلال از مقیاس:

| کل نورون | لوب پایانی | **دهان** | **گوش الف** | **گوش ب** |
|---|---|---|---|---|
| ۵٬۰۰۰ | ۹۹۰ | **۲۴** | **۴۸** | **۴۸** |
| ۳۲٬۰۰۰ | ۶٬۳۴۲ | **۲۴** | **۴۸** | **۴۸** |
| ۱۰۰٬۰۰۰ | ۱۹٬۸۲۰ | **۲۴** | **۴۸** | **۴۸** |

لوب پایانی بیست برابر شد، دهان دست‌نخورده ماند.

### ۲۶٫۲ اصلاح دوم: گوش‌ها هم باید مجموعه‌ی مشخص باشند

پیش‌تر تزریق ورودی این‌طور بود: «به ۶۴ نورون اول لوب ورودی که در پیمایش دیده شدند». یعنی مجموعه‌ی گیرنده نه از پیش تعیین‌شده بود و نه پایدار — با مرگ نورون‌ها عوض می‌شد.

> حالا «گوش‌ها» هنگام ساخت مغز یک بار انتخاب می‌شوند و در `brain.dat` ذخیره می‌شوند. همان نورون‌ها همیشه می‌شنوند.

این برای یادگیری حیاتی است: اگر هر بار نورون دیگری ورودی بگیرد، هیچ نورونی نمی‌تواند در شنیدن تخصص پیدا کند.

### ۲۶٫۳ نتیجه در مقیاس ۳۲٬۰۰۰

| سنجه | مقدار |
|---|---|
| زنده | ۳۱٬۶۰۹ از ۳۲٬۰۰۰ |
| مرگ اولیه | ۳۹۱ (۱٫۲٪) سپس پایدار |
| استخر | ورودی ۱۷٪ · مرکزی ۲۲٪ · پایانی ۱۹٪ |
| نرخ فایر | ۴٫۱ هرتز |
| کلمه در ثانیه | ۰٫۱۹ |
| رویداد در ثانیه | ~۴٫۵ میلیون |

ورودی انسان تست شد: پیام «سلام» از راه ۴۸ گوش تزریق شد و مغز پایدار ماند.

---

## ۲۷. تنظیم نرخ گفتار + کنترل زنده

پس از بند ۲۶ نرخ به ۱۴ کلمه در دقیقه رسید — بهتر از ۱۴۵ کلمه در **ثانیه**، ولی هنوز برای نمره‌دهی دقیق زیاد.

### ۲۷٫۱ مشکل پنهان: همه‌ی کلمات دقیقاً ۱۰ حرف بودند

نگاه به خروجی نشان داد هر کلمه **دقیقاً** به سقف طول می‌خورد:

```
؟ت۹-ث۶عصغ؟    یگغگةئةدا.    !غء!صق!غکک    ی:۹ءکئخجع؟
```

یعنی مدل **هرگز خودش کلمه را تمام نمی‌کرد** — سقف اجباری می‌بستش. پایان کلمه یک تصمیم نبود، یک تصادف بود.

پس از تنظیم (دهان ۲۴→۱۴، تعلیق ۱۲→۱۵ برابر، سقف طول ۱۰→۱۲) طول‌ها متنوع شدند:

```
ئطط    د    .کضقةبات۶ج.ی    ظظبب.؟شظخ؟یش
```

کلمه‌ی یک‌حرفی، سه‌حرفی، و بلند — یعنی **پایان کلمه حالا واقعاً از تصمیم مدل می‌آید**.

### ۲۷٫۲ اسلایدر پرحرفی

به‌جای عدد ثابت در کد، کنترل زنده در داشبورد:

```
پرحرفی: ۱۰٪ ────────●──────── ۴۰۰٪
```

روی تعلیق نورون‌های دهان اثر می‌گذارد:

```
تعلیق دهان = REFRACTORY × 15 × 100 / پرحرفی
```

| تنظیم | نرخ اندازه‌گیری‌شده |
|---|---|
| ۳۰٪ | ۳٫۴ کلمه در دقیقه |
| ۱۰۰٪ | ۷٫۸ کلمه در دقیقه |

حالا بدون کامپایل مجدد می‌شود نرخ را با سرعت خواندن خود تطبیق داد.

### ۲۷٫۳ چرا این پارامتر باید دست انسان باشد

نرخ مناسب به کاری بستگی دارد که در آن لحظه می‌کنیم:
- **نمره‌دهی دقیق دستی** → ۳ تا ۵ کلمه در دقیقه
- **رصد رفتار کلی** → ۲۰ تا ۵۰
- **فاز ۲ به بعد (نمره‌دهی خودکار با دیکشنری)** → هر چه بیشتر، بهتر

در فازهای بعدی که معلم خودکار می‌شود، این اسلایدر تا انتها باز خواهد شد.

---

## ۲۸. موازی‌سازی و کنترل مصرف پردازنده

تا اینجا موتور تک‌نخ بود. بند ۱۲٫۱ راه موازی‌سازی را از قبل مشخص کرده بود؛ حالا پیاده شد.

### ۲۸٫۱ موتور پنجره‌ی موازی

طبق بند ۱۲٫۱، چون حداقل تأخیر یال ۱ms است، هیچ رویدادی در بازه‌ی `[t, t+1ms)` نمی‌تواند روی رویداد دیگری در همان بازه اثر بگذارد.

```
۱. همه‌ی رویدادهای پنجره یکجا برداشته می‌شوند
۲. سیگنال‌ها و سیستم‌تیک ترتیبی اجرا می‌شوند (ارزان، باید مرتب باشند)
۳. ارزیابی نورون‌ها بین نخ‌ها تقسیم می‌شود ← موازی
۴. هر نخ خروجی را در بافر محلی می‌ریزد (بدون قفل)
۵. ادغام به ترتیب ثابت نخ‌ها ← تکرارپذیری حفظ می‌شود
```

**تکرارپذیری تأیید شد:** بذر ۴۲ با ۱، ۲ و ۴ نخ دقیقاً `fires=325072 signals=897974`.

### ۲۸٫۲ سه باگ کارایی که موتور را از کار انداخته بود

| باگ | اثر | اصلاح |
|---|---|---|
| `std::sort` روی صف ترانزیت در هر پنجره | ~۵۰۰ هزار عنصر × ۱۰۰۰ بار در ثانیه — موتور عملاً متوقف | حذف شد؛ ورودی‌های هر پنجره مهر زمانی یکسان دارند و صف ذاتاً مرتب است |
| پیمایش کل جمعیت برای ثبت مرگ در هر پنجره | ۳۲ هزار × ۱۰۰۰ = ۳۲ میلیون بررسی در ثانیه | هر ۵۰ پنجره یک بار |
| ساخت نخ در هر پنجره | ~۲۰µs ساخت در برابر کار ناچیز | استخر نخ ماندگار |

### ۲۸٫۳ استخر نخ با خواب واقعی

نسخه‌ی اول نخ‌ها را با `yield` می‌چرخاند — نتیجه ۱۰۰٪ مصرف CPU حتی وقتی کاری نبود، و محدودکننده بی‌اثر می‌شد.

> با `condition_variable` نخ‌ها **واقعاً می‌خوابند** تا کار برسد.

### ۲۸٫۴ محدودکننده‌ی CPU

نسبت کار به استراحت در پایان هر پنجره:

```
مدت خواب = مدت کار × (۱۰۰ − سقف) ÷ سقف
```

اندازه‌گیری روی ماشین دوهسته‌ای:

| سقف | مصرف واقعی |
|---|---|
| ۱۰۰٪ | ۱۵۷٪ (دو هسته) |
| ۵۰٪ | ۷۳٪ |
| ۲۵٪ | ۳۶٪ |

رابطه خطی است.

### ۲۸٫۵ کنترل‌ها

**داشبورد:** اسلایدر «سقف CPU» (۱۰–۱۰۰٪) · فیلد «نخ» (۰ = خودکار) · کارت زنده‌ی مصرف

**خط فرمان:** `--threads N` (۰ = همه‌ی هسته‌ها) · `--cpu N` (۱۰–۱۰۰)

### ۲۸٫۶ انتظار واقع‌بینانه از شتاب

سنجش روی ماشین دوهسته‌ای شتاب چندانی نشان نداد. علت ساختاری است: **هر پنجره ~۹۰۰ رویداد دارد** و هزینه‌ی همگام‌سازی در این اندازه با سود موازی‌سازی رقابت می‌کند.

سود واقعی جایی ظاهر می‌شود که:
- تعداد نورون بسیار بیشتر باشد (پنجره‌های پرتر)
- هسته‌های بیشتری موجود باشد (۸ به بالا)
- کار هر نورون سنگین‌تر شود (بایت‌کد پیچیده‌تر در فازهای بعد)

زیرساخت اکنون آماده است؛ روی سخت‌افزار چندهسته‌ای واقعی خودش را نشان می‌دهد.

### ۲۸٫۷ پروفایل واقعی و حذف صف دودویی

گزارش ۳۵–۴۰٪ مصرف روی یک CPU چند‌هسته‌ای نشان داد «ساختن همه‌ی نخ‌ها» به‌معنای داشتن کار موازی برای همه نیست. پروفایل ۳۲ هزار نورون مشخص کرد:

```text
neuron_eval_mt                 69.8٪  ← قابل موازی‌سازی
priority_queue::pop            26.5٪  ← کاملاً ترتیبی
```

چون زمان مجازی فقط رو به جلو می‌رود، صف رویداد با **radix heap یکنواخت** جایگزین شد. ترتیب دقیق `(زمان، seq)` حفظ شده و اثرانگشت ۱، ۲ و ۴ نخ همچنان یکسان است. `seq` نیز از ۳۲ به ۶۴ بیت رفت؛ شمارنده‌ی قبلی با نرخ چند میلیون رویداد بر ثانیه در اجرای دائم خیلی زود سرریز می‌کرد.

پس از تغییر، سهم مدیریت صف از ۲۶٫۵٪ به حدود ۱۰٪ رسید. روی ماشین آزمایش دوهسته‌ای:

| ۳ ثانیه‌ی مجازی، ۳۲k | صف دودویی | رادیکس | تغییر |
|---|---:|---:|---:|
| ۱ نخ | ۸٫۰۳s | ۷٫۱۶s | حدود ۱۱٪ سریع‌تر |
| ۲ نخ | میانگین ۴٫۲۰s برای آزمون ۲s | ۳٫۹۱s | حدود ۷٪ سریع‌تر |

این هنوز مصرف همه‌ی هسته‌های یک CPU بزرگ را تضمین نمی‌کند؛ سد ۱ms، ادغام خروجی و تحویل ترتیبی سیگنال‌ها گلوگاه بعدی‌اند. هدف درست «۱۰۰٪ نشان‌دادن Task Manager» نیست، بلکه بیشینه‌کردن ثانیه‌ی مجازی بر ثانیه‌ی واقعی است.

سنجه‌ی داشبورد نیز اصلاح شد: مقدار قبلی duty-cycle نخ هماهنگ‌کننده بود، نه مصرف فرایند. اکنون زمان CPU جمع همه‌ی نخ‌ها از سیستم‌عامل خوانده، بر تعداد پردازنده‌های منطقی نرمال و کنار **سرعت زمان مجازی (×)** نمایش داده می‌شود.

---

## ۲۹. فاز ۲ — معلم خودکار فارسی و اعتبار علّی

آزمایش دستی نشان داد شبکه می‌تواند رفتارهایی را که مانا می‌گیرند حفظ کند، اما پهنای باند انسان گلوگاه شد. راه‌حل این فاز یک **دستگاه داور بیرونی** است؛ داور حق ندارد حافظه، برنامه یا سیم‌کشی نورون را مستقیم تغییر دهد و فقط همان سیگنال مانایی را تولید می‌کند که کلیک انسان تولید می‌کرد.

### ۲۹٫۱ فایل داده‌ی مستقل

کد در `smile.cpp` است و داده دو لایه‌ی UTF-8 TSV دارد:

```text
persian_words.tsv   ← پایه‌ی curated و فقط‌خواندنی
my_words.tsv        ← پیشنهادها/اصلاح‌های انسان؛ هنگام آپدیت حفظ می‌شود

واژه<TAB>فراوانی<TAB>وضعیت<TAB>منبع یا توضیح
```

نسخه‌ی اول اشتباه بود: ۱۵۵٬۶۰۲ type خام زیرنویس با شرط ضعیف فراوانی ≥۲ وارد شده بود؛ **۳۹٬۴۰۱ ردیف فقط دو بار دیده شده بودند** و شامل کلمه‌ی چسبیده، اسم خارجی و غلط تایپی می‌شدند. frequency به‌اشتباه نقش ضمنی «اعتبار» گرفته بود.

نسخه‌ی اصلاح‌شده شامل **۸۱٬۰۶۳ واژه‌ی دارای برچسب دستوری** از Lilak Persian Spell Checking Dictionary (Apache-2.0) است. ۱۲٬۶۲۴ ورودی untagged/user نیز برای احتیاط حذف شدند. فراوانی زیرنویس فقط وزن آماری واژه‌های تاییدشده است و دیگر حق عضویت نمی‌دهد. فایل بر اساس الفبای فارسی مرتب شده، نیم‌فاصله را برای خوانایی انسان نگه می‌دارد و شکل‌های عربی/همزه‌ای خارج از کدک ساده حذف شده‌اند.

وضعیت‌های `my_words.tsv`:

- `verified`: وارد دیکشنری و مدل املایی شود؛
- `suggested`: فقط برای بازبینی ثبت شود و **هیچ آموزشی ندهد**؛
- `blocked`: حتی اگر در پایه هست حذف شود.

اگر فایل پایه موجود نباشد برنامه از کار نمی‌افتد؛ معلم خودکار خاموش می‌شود و داوری دستی باقی می‌ماند.

### ۲۹٫۲ چرا دیکشنری صفر و یکی کافی نیست

در شروع، احتمال اینکه خروجی تصادفی دقیقاً عضو واژه‌نامه باشد بسیار پایین است. اگر داور فقط «عضو = مثبت / غیرعضو = منفی» بدهد، تقریباً تمام تجربه‌ها منفی می‌شوند و شبکه سکوت را یاد می‌گیرد.

هنگام بارگذاری فایل، دو جدول از خود واژه‌ها ساخته می‌شود:

- مدل دوحرفی ۶۵×۶۵؛
- مدل سه‌حرفی ۶۵×۶۵×۶۵؛
- نماد ۶۴ مرز آغاز/پایان واژه است؛
- وزن هر واژه ریشه‌ی دوم فراوانی آن است تا چند واژه‌ی بسیار پرتکرار کل مدل را نبلعند.

حالت‌های داور:

| حالت | سیگنال |
|---:|---|
| ۰ | خاموش |
| ۱ | شباهت املایی پیوسته با مدل دو/سه‌حرفی |
| ۲ | عضویت + فراوانی دیکشنری |
| ۳ | ۵۵٪ املایی + ۴۵٪ دیکشنری |

نمره‌ی خام ۰ تا ۱۰۰ است. سیگنال مانا از اختلاف آن با خط پایه‌ی نمایی متحرک ساخته می‌شود؛ بنابراین در آغاز حتی «کمتر بد بودن» قابل یادگیری است و میانگین بلندمدت باعث تورم دائمی مانا نمی‌شود.

### ۲۹٫۳ مهار تقلب داور

چند میان‌بُر از ابتدا بسته شد:

- سه حرف یکسان پشت‌سرهم جریمه می‌شود؛
- frequency هرقدر بالا باشد به‌تنهایی عضویت نمی‌سازد؛ فقط `verified` معتبر است؛
- `suggested` نه پاداش دیکشنری می‌گیرد و نه n-gram را آلوده می‌کند؛
- `blocked` عضویت پایه را لغو می‌کند؛
- تکرار همان واژه در خروجی‌های اخیر جریمه‌ی افزایشی دارد؛
- نشانه‌گذاری وسط واژه مردود و نشانه‌ی پایانی اندکی جریمه می‌شود.

این‌ها کامل نیستند؛ داشبورد باید نرخ واژه‌ی دقیق، کیفیت و تکرار را نشان دهد تا هر reward hack تازه دیده شود.

### ۲۹٫۴ ردپای علّی — اصلاح یک نقص بنیادی

نسخه‌ی قبل نمره را فقط وارد استخر کل لوب می‌کرد. در نتیجه نورون سازنده‌ی واژه‌ی خوب و نورون نامرتبط هم‌لوب هر دو سود می‌بردند. اکنون هر رویداد سیگنال شناسه‌ی فرستنده را حمل می‌کند و هر خط ورودی آخرین فرستنده و زمانش را نگه می‌دارد.

هنگام تولید هر نماد، دستگاه از نورون دهان با این قاعده عقب می‌رود:

```text
عمق حداکثر: ۷ یال
پنجره‌ی تازگی هر یال: ۵۰ms
سقف ردپای یک واژه: ۷۶۸ نورون
```

ردپا همراه `OutWord` منجمد می‌شود. پاداش/تنبیه:

1. اقتصاد کلان را از راه استخر لوب‌ها تغییر می‌دهد؛
2. اعتبار را فقط در نورون‌های همان ردپا تغییر می‌دهد؛
3. وزن اعتبار در لوب پایانی ۱، مرکزی ۰٫۶۵ و ورودی ۰٫۴ است.

این backprop عددی نیست؛ رسید محلی زنجیره‌ی واقعی ارسال سیگنال است. نمره‌ی دستی مستقل و بسیار قوی‌تر از معلم خودکار باقی مانده و اگر انسان نمره‌ی خود را تغییر دهد فقط اختلاف نمره‌ی دستی دوباره اعمال می‌شود.

### ۲۹٫۵ شدت محافظه‌کارانه

قدرت پیش‌فرض معلم ۳۵٪ است و سیگنال اعتباری هر واژه در بازه‌ی تقریبی `−0.5 .. +0.5` قطع می‌شود؛ در عمل معمولاً حدود `±0.05 .. ±0.2` است. فشار کامل به اعتبار مسیر علّی می‌رسد، اما فقط **یک‌هشتم** آن وارد استخر کل لوب می‌شود تا موج خطاهای ابتدای آموزش، سکوت دائمی را به‌عنوان راه فرار نسازد. اولین واژه‌ی هر نوع معلم نیز فقط خط پایه‌ی مستقل همان نوع را مقداردهی می‌کند و میانگین متحرک با ضریب ۰٫۰۸ سریع تطبیق می‌یابد. این در برابر فرمان دستی `±10` عمداً کوچک است تا هزاران داوری خودکار اقتصاد را نابود نکند.

داشبورد اکنون نشان می‌دهد:

- تعداد داوری خودکار و دستی؛
- تعداد اعضای دقیق واژه‌نامه؛
- میانگین کیفیت؛
- پاداش خالص خودکار؛
- کیفیت، عضویت و اندازه‌ی ردپای هر واژه در tooltip؛
- انتخاب حالت و قدرت معلم در زمان اجرا.

### ۲۹٫۶ شفافیت کامل داور

«کیفیت زبان» و «سیگنال مانا» دو عدد متفاوت‌اند و مخلوط‌کردن آن‌ها تشخیص رفتار معلم را غیرممکن می‌کرد. برای هر `OutWord` اکنون این رسید نگه‌داری و در داشبورد نمایش داده می‌شود:

| ستون | معنا |
|---|---|
| املا | کیفیت آماری دو/سه‌حرفی، ۰ تا ۱۰۰ |
| دیکشنری | عضویت و فراوانی واژه، ۰ تا ۱۰۰ |
| کیفیت نهایی | خروجی معلم انتخاب‌شده پس از جریمه‌ی تکرار/نشانه‌باران |
| خط پایه | EMA کیفیت پیش از دیدن همین واژه |
| مزیت | اختلاف کیفیت با خط پایه، پس از کف واژه‌ی دقیق و clamp |
| سیگنال معلم | `clamp(مزیت × قدرت ÷ ۸۰۰۰, −۰٫۵, +۰٫۵)`؛ کد ابتدا همین را در واحد میلی‌سیگنال حساب می‌کند |
| نمره‌ی دستی | سیگنال مستقل انسان؛ جای سیگنال خودکار را پنهان نمی‌کند |
| ردپا | تعداد نورون‌های رسید علّی همان واژه |

داشبورد یک جدول ریز برای ۲۵ واژه‌ی آخر دارد. علاوه بر میانگین کلی کیفیت، نرخ عضویت دیکشنری و شمار مثبت/منفی/خنثی، برای هر حالت املایی، دیکشنری و ترکیبی **خط پایه‌ی مستقل**، تعداد نمونه، میانگین کیفیت و میانگین سیگنال نگه داشته می‌شود. تغییر حالت معلم بنابراین نه baseline و نه تاریخچه‌ی آماری حالت قبلی را در حالت جدید حل نمی‌کند.

### ۲۹٫۷ آیا معلم واقعاً رفتار را بهتر می‌کند؟

آزمون A/B اولیه جواب ناخوشایندی داد: با همان seed و ۳۵۰ ثانیه‌ی مجازی، `teacher-strength=0` و `35` **اثرانگشت کاملاً یکسان** داشتند. اعتبار فقط سهم برداشت مانا را تغییر می‌داد و در اقتصاد سیر عملاً هیچ تصمیم فایری عوض نمی‌شد. پس ادعای «آموزش» در آن نسخه درست نبود؛ فقط داوری انجام می‌شد.

اصلاح: هر نورون یک `plasticity` علامت‌دار و آهسته دارد که فقط از ردپای علّی تغییر می‌کند:

- مثبت → cadence و refractory کمتر، فرصت فایر بیشتر؛
- منفی → cadence/refractory بیشتر و گیت احتمالی فایر؛
- بازه `−8192..+8192`، فراموشی آهسته‌ی یک‌ساعته؛
- در checkpoint نسخه‌ی ۵ ذخیره می‌شود؛ نسخه‌های قدیمی با مقدار صفر بار می‌شوند.

یک A/B اولیه روی دیکشنری آلوده ظاهراً بهبود نشان داد، اما بعد از پاک‌سازی واژه‌نامه نتیجه تکرار نشد؛ یعنی خود داده‌ی بد، معیار را هم فریب داده بود. A/B نهایی فعلی روی سه seed، ۱۰۰۰ نورون و ۳۵۰ ثانیه‌ی مجازی:

| سنجه‌ی تجمیعی | فقط داوری، قدرت ۰ | plasticity، قدرت ۳۵ |
|---|---:|---:|
| واژه | ۲۱۱ | ۲۰۱ |
| واژه‌ی exact | ۵٫۲۱٪ | ۴٫۹۸٪ |
| میانگین کیفیت | ۱۲٫۰۹ | ۱۱٫۴۳ |
| میانگین کیفیت ۱۰ واژه‌ی آخر | ۹٫۰۳ | ۱۰٫۲۰ |

plasticity رفتار را واقعاً تغییر داد (اثرانگشت‌ها متفاوت شدند)، اما **پیشرفت قابل اتکا ایجاد نکرد**: exact و میانگین کل کمی بدتر شدند و فقط پنجره‌ی آخر اندکی بهتر شد. بنابراین قدرت پیش‌فرض معلم به **۰** تغییر کرد؛ در این حالت داور همه‌ی نمره‌ها را نمایش می‌دهد ولی خودکار مغز را دستکاری نمی‌کند. قدرت بالاتر فقط آزمایشی است.

نتیجه‌ی صادقانه: این نسخه اکنون **داور تمیز و قابل اندازه‌گیری** دارد، نه سیستم آموزش زبانی اثبات‌شده. مرحله‌ی بعد برای یادگیری واقعی باید وزن/کارایی یال، حافظه‌ی زمینه‌ای و جایگزینی/جهش ساختاری را وارد A/B کند؛ زیادکردن شدت همین سیگنال راه‌حل نیست.

---

## ۳۰. آزمون CUDA ناشناس — ۱۲۸ هزار نورون

این آزمون عمداً پیش از انتقال کامل میزبان ساخته شد تا روی سخت‌افزار واقعی GTX به چهار سؤال پاسخ دهد:

1. آیا VM خطی نورون‌های عادی و نیمه‌خطی حافظه‌ای روی GPU درست اجرا می‌شود؟
2. حلقه‌ی سیگنال با تأخیر ۱ تا ۲۰ میلی‌ثانیه چه توان عملی‌ای دارد؟
3. با فقط ۱۲۸٬۰۰۰ نورون، درصد واقعی استفاده از GPU چقدر است؟
4. آیا کنترل سقف استفاده بدون جعل بار مصنوعی کار می‌کند؟

### ۳۰٫۱ فایل و دامنه

`smile_cuda.cu` یک مسیر CUDA واقعی اما آزمایشی است. این اجزا را اجرا می‌کند:

- همان بایت‌کدهای بذر عادی و حافظه‌ای؛
- نسبت واقعی ۹۶٫۸۵٪ عادی، ۳٪ حافظه‌ای و ~۰٫۱۵٪ غول؛
- عادی/حافظه‌ای روی GPU، غول‌ها در شمارنده‌ی کار میزبان؛
- ۲۰/۴۰ یال واقعی و تأخیر ۱ تا ۲۰ms؛
- پنجره‌ی وابستگی ۱ms و CUDA Graph در صورت پشتیبانی؛
- مانای نورونی، هزینه‌ی فایر، هزینه‌ی نگهداری، استخر لوب و بازگشت ۵۰۰ms؛
- حافظه‌ی ۳۲ بایتی/۱KiB و VM با حلقه‌ی بازشده؛
- قیف ثابت ۱۴ نورون دهان.

این یک ضرب ماتریس یا حلقه‌ی بی‌معنی برای بالا بردن Task Manager نیست. هر تکرار، زمان مجازی مغز را واقعاً جلو می‌برد.

### ۳۰٫۲ مرز صادقانه‌ی آزمون

این نسخه هنوز جایگزین `smile.cpp` نیست. موارد زیر تا تأیید سخت‌افزار در مسیر CPU کامل باقی می‌مانند:

- داشبورد و معلم فارسی؛
- ردپای علّی دقیق نمره؛
- تبادل سیگنال واقعی غول CPU با توده‌ی GPU؛
- چک‌پوینت کامل حالت GPU.

پس معیار این مرحله **درستی/توان موتور توده** است، نه آموزش زبانی انتهابه‌انتها.

### ۳۰٫۳ سقف ۷۰٪

`--gpu-limit 70` سقف است، نه هدفی که با محاسبه‌ی جعلی پر شود. NVML هر حدود نیم‌ثانیه utilization را می‌خواند و duty-cycle را تنظیم می‌کند:

- بالاتر از ۷۲٪ → کاهش duty؛
- پایین‌تر از ۶۶٪ → افزایش duty تا ۱۰۰٪؛
- اگر ۱۲۸k ذاتاً سبک باشد، مقدار واقعی پایین‌تر می‌ماند؛
- نبود NVML → محدودیت محافظه‌کارانه بر اساس نسبت کار/خواب؛
- ۸۵°C → duty حداکثر ۲۵٪؛ ۹۰°C → توقف حرارتی.

### ۳۰٫۴ سازگاری GTX

CUDA 13 امکان offline compilation برای compute capability پایین‌تر از ۷٫۵ را حذف کرده است. بنابراین:

| خانواده | compute | Toolkit ساخت |
|---|---:|---|
| GTX 900 (Maxwell) | 5.x | CUDA 12.9 |
| GTX 10 (Pascal) | 6.1 | CUDA 12.9 |
| GTX 16 (Turing) | 7.5 | CUDA 12.9 یا 13.x |

`run.ps1` مقدار `compute_cap` را از `nvidia-smi` می‌خواند، `sm_XX` بومی می‌سازد و ترکیب CUDA 13 + pre-Turing را پیش از کامپایل رد می‌کند.

---

## ۳۱. آزمون تاریخی مقیاس ۱۲۸٬۰۰۰

در یک مرحله‌ی آزمایشی، پیش‌فرض ۳۲k موقتاً با ۱۲۸k جایگزین شد. نتایج این بخش برای مقایسه‌ی مقیاس حفظ شده‌اند؛ بخش ۳۲ وضعیت عملیاتی فعلی را مشخص می‌کند. پهنای I/O در این آزمون ثابت ماند:

| بخش | تعداد |
|---|---:|
| کل | ۱۲۸٬۰۰۰ |
| عادی | ۱۲۳٬۹۶۸ |
| حافظه‌ای | ۳٬۸۴۰ |
| غول | ۱۹۲ |
| لوب ورودی | ۲۵٬۳۷۰ |
| لوب مرکزی | ۷۷٬۲۶۱ |
| لوب خروجی | ۲۵٬۳۶۹ |
| دهان | **۱۴ ثابت** |
| گوش انسان | **۴۸ ثابت** |
| گوش آینه | **۴۸ ثابت** |
| یال واقعی ساخته‌شده | ۲٬۶۴۰٬۵۸۴ |

آزمون CPU روی sandbox دو پردازنده‌ی منطقی:

| سنجه | نتیجه |
|---|---:|
| peak RSS | ~۳۴۲ MiB |
| اندازه‌ی `brain.dat` | ۵۳٬۵۱۰٬۶۹۲ بایت (~۵۱ MiB) |
| ساخت + ۱ ثانیه‌ی مجازی + ذخیره | ۹٫۷ ثانیه wall |
| ساخت + ۲ ثانیه‌ی مجازی + ذخیره | ۱۶٫۵ ثانیه wall |
| مرگ در ۱۰ ثانیه‌ی مجازی | صفر |
| fault در ۱۰ ثانیه | صفر |
| fire / signals / events در ۱۰s | ۷٫۹۶M / ۲۲٫۲۳M / ۱۸۰٫۵۱M |
| نرخ فایر انتهای آزمون | ~۵٫۶۱ Hz/نورون |
| میانگین استخر انتهای آزمون | ~۴۰٪ |
| wall برای آزمون ۱۰s | ۷۴٫۴ ثانیه |
| CPU aggregate | ~۱۵۸٪ از ۲۰۰٪ موجود |

این زمان شامل ساخت ۲٫۶۴ میلیون یال و نوشتن checkpoint هم هست، پس نرخ steady-state کمی بهتر از نسبت خام است. با این حال روی CPU دو-thread فقط حدود `0.13×` زمان مجازی حاصل شد؛ بنابراین اجرای بی‌درنگ ۱× روی این سخت‌افزار ممکن نیست. مقیاس ۱۲۸k پایدار ساخته و اجرا می‌شود، ولی بدون GPU یا CPU قوی، آموزش دیواری کند خواهد بود.

runner همان مرحله در اولین اجرای ۱۲۸k، `brain.dat` قبلی را آرشیو می‌کرد. این رفتار تاریخی است و با سیاست اندازه‌ی جاری در بخش بعد جایگزین شده است.

---

## ۳۲. بازگشت پیش‌فرض عملیاتی به ۳۲٬۰۰۰ نورون

برای اجرای روان‌تر روی CPU، پیش‌فرض هر دو مسیر CPU و CUDA دوباره **۳۲٬۰۰۰ نورون واقعی** است:

| بخش | تعداد |
|---|---:|
| کل | ۳۲٬۰۰۰ |
| عادی | ۳۰٬۹۹۲ |
| حافظه‌ای | ۹۶۰ |
| غول | ۴۸ |
| یال واقعی | ۶۶۰٬۱۲۴ |
| دهان | **۱۴ ثابت** |
| گوش انسان | **۴۸ ثابت** |
| گوش آینه | **۴۸ ثابت** |

اعتبارسنجی نسخه‌ی جاری روی دو پردازنده‌ی منطقی: ساخت مغز، اجرای یک ثانیه‌ی مجازی و ذخیره در `3.1 s` دیواری انجام شد؛ peak RSS حدود `98 MiB`، اندازه‌ی checkpoint برابر `13,378,536` بایت، رویدادها `4,727,575` و fault برابر صفر بود.

`run.ps1` اندازه را در marker واحد `.brain-size-current` ذخیره می‌کند. هنگام تغییر اندازه، checkpoint قبلی را با نام `brain-before-32k-*.dat` نگه می‌دارد و مغز تمیز ۳۲k می‌سازد. خود موتور نیز checkpoint با تعداد نورون متفاوت از `--neurons` را رد می‌کند؛ بنابراین markerهای قدیمی نمی‌توانند بی‌صدا مغز ۱۲۸k را دوباره بارگذاری کنند.
